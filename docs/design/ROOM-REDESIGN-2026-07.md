---
tags: [design, room, redesign]
---

# ROOM REDESIGN 2026-07 — 원형 무대 (컨셉 → 구현 스펙 · 와이어프레임 1패스)

> 목적: `DESIGN-DIRECTION.md §6`이 "원형 배치의 정밀 와이어프레임 패스가 아직 없음 — 구현 착수 전 한 번 더 필요 (PENDING)"이라 못박은 그 패스를 닫는다.
> 시각 SSOT = 사용자 제공 컨셉 이미지 `~/Documents/채터박스/chatterbox_room_concept.png` (2026-07-09).
> 존 스펙 = `DESIGN-DIRECTION.md §6`. 팔레트/모션 = `design/DESIGN-TOKENS.md §8`(창작 금지, 그대로 재사용).

## 0. BLUF

리디자인 = **레이아웃 셸 재편(2열 → 3존) + 기존기능 재배선 + 미빌드 컨셉요소는 시각 스텁/defer.**
컨셉의 ~10개 요소 중 실제 코드에 있는 건 절반(스크립트·리액션·마이크·나가기·메인뷰·탭·네트워크·씬배경·ProgressBar). 나머지 절반(분위기 지수·사운드보드 SFX오디오·룸 녹음·다음순서 큐·카메라/헤드폰 토글·채팅 오버레이 버블·영상 스크러버)은 **백엔드 신규**라 이번 MVP는 "컨셉처럼 보이되 기존기능이 새 셸에서 다 동작" + 미빌드는 시각 스텁으로 자리만 잡는다. 4 페이즈: **R1 셸+상/하단바 → R2 원형무대 → R3 좌측도크 → R4 우측도크.**

## 1. 레이아웃 셸

```
┌───────────────────── 상단바 (~64px) ──────────────────────────┐
│ [로고 Beta] 제목 #태그  · ● 라이브 00:00:00 ·  초대  링크  5/6  ⋮ │
├──────────┬──────────────────────────────────┬──────────┤
│ 좌측도크  │        원형 무대 (1fr)            │ 우측도크  │
│ 240–280 │  아바타6 ⟲ 별자리글로우 ⟲ 씬히어로  │  300px   │
│ (스크롤) │  중앙 메인뷰 + 발화자 링/뱃지     │ (스크롤)  │
├──────────┴──────────────────────────────────┴──────────┤
│           하단 컨트롤바 (~76px)                            │
│  🎙마이크  🎧헤드폰  ⏺녹음  👤아바타  📖스크립트  😊리액션  →나가기 │
└───────────────────────────────────────────────────────┘
```

- **그리드:** `grid-rows: auto 1fr auto` / main `grid-cols: clamp(240px,20vw,280px) 1fr 300px`, `gap-3`.
- **반응형(§CODING-CONVENTIONS DoD):** `<1024px` 도크는 오프캔버스 드로어(상단바 토글). 모바일(`<768`)은 뷰어 경로(MOB-01) — 무대만, 도크·하단바 축약. 360px 오버플로 0.
- **토큰:** 배경 `stage-base`, 도크/바 `stage-panel`, 카드/모달 `stage-elevated`, 경계 `stage-border`, 본문 `stage-text`/보조 `stage-text-muted`. 액센트 `fire-amber`(호스트·활성·CTA만), `fire-hot`(라이브·녹음), `spring-green`(트래킹·ON). **앰버 배경워시 금지.**

## 2. 존 분해 (컨셉요소 → 현재코드 → MVP 판정)

| 존 | 컨셉 요소 | 현재 코드 | MVP | effort | 비고 |
|---|---|---|---|---|---|
| 상단바 | 로고·제목·#태그 | 부분(방 제목) | ✅ | S | 태그=장르 |
| 상단바 | ● 라이브 + 타이머 | 없음 | ✅ | S | 클라 타이머(입장 기준 경과, room started_at defer) |
| 상단바 | 초대하기·링크공유 | `createRoomInvite`(HostConsole) | ✅ | S | 상단으로 승격 |
| 상단바 | 5/6·⋮메뉴 | 참가자수 O / 메뉴 X | ✅ | S | ⋮ = HostConsole 기능(배경·잠금·강퇴) |
| 상단바 | 네트워크 인디케이터 | `NetworkStatusIndicator` | ✅ | S | 위치 이동 |
| 무대 | 원형 아바타6+씬 히어로 | `Stage`(3×3)·씬배경O | ✅ | M | 사각→원형·재배치 |
| 무대 | 호스트 앰버링+크라운 | 없음(호스트 판정O) | ✅ | S | 시각만 |
| 무대 | 발화자 글로우+웨이브+뱃지 | `isSpeaking` O | ✅ | S~M | ring/scale/뱃지 |
| 무대 | 아바타별 마이크 아이콘 | mic 상태 O | ✅ | S | |
| 무대 | 트래킹 도트(green) | 트래킹 상태 O | ✅ | S | |
| 무대 | 별자리 글로우 라인 | 없음 | ✅(lite) | M | CSS/SVG 저강도, 풀 애니 defer |
| 무대 | 중앙 영상 컨트롤(스크러버) | `MainView`(공유재생) | ⛔ defer | M | 스크럽 UI 신규 |
| 무대 | 채팅 오버레이 버블(불씨) | **없음**(ChatOverlay MISSING) | ⛔ defer | M | §7 신규 컴포넌트 |
| 좌측 | 스크립트/현재 장면 | `ScriptPanel` O | ✅ | S | 좌측 도크로 |
| 좌측 | 세션 정보(장르·러닝·언어) | 파생 가능 | ✅ | S | 오디오포맷=정적 |
| 좌측 | 다음 순서(큐) | 없음(손들기 큐는 별개) | ⛔ defer | M | 배우 턴오더 로직 신규 |
| 우측 | 라이브 노트 | 채팅 O(chat 탭) | ✅ | S | 채팅=노트 재사용 |
| 우측 | 사운드보드 2×3 | `sendReaction` O·SFX 오디오 X | ✅(시각)/⛔(SFX) | M | 그리드 클릭→기존 리액션. SFX 오디오 defer |
| 우측 | 방 분위기 지수 87% | 없음 | ⛔ defer | L | mood 백엔드 없음. ProgressBar로 시각 스텁만 |
| 하단바 | 마이크·스크립트·리액션·나가기 | `toggleMic`·ScriptPanel·ReactionWheel·`leave` O | ✅ | S | 실배선 |
| 하단바 | 헤드폰·녹음·아바타/카메라 토글 | 없음(룸레벨) | ⛔ defer | M~L | 시각 present, 배선은 후속 |

## 3. 페이즈 플랜 (phase-loop, 각 페이즈 게이트 통과)

- **R1 — 셸 + 상/하단바:** `RoomPage`를 3존 그리드로 재편. 상단바(제목·태그·라이브타이머·초대·링크·N/6·⋮·네트워크), 하단 컨트롤바(마이크·스크립트·리액션·나가기 실배선 / 헤드폰·녹음·아바타 시각). **골격.**
- **R2 — 원형 무대:** 3×3 → 원형 3쌍 배치, 원형 아바타, 호스트 앰버링+크라운, 발화자 글로우+웨이브+뱃지, 마이크/트래킹 도트, 씬배경 히어로화, 별자리 글로우(lite).
- **R3 — 좌측 도크:** `ScriptPanel` 이식 + 세션정보(파생). [다음순서 큐 = defer]
- **R4 — 우측 도크:** 라이브노트(채팅) + 사운드보드 그리드(기존 리액션 재사용) + 분위기 지수(시각 스텁). 기존 DUB/VGen/관리 탭 보존.

> **as-built (2026-07-10, Fable — 인수 phase-loop):** R1 셸 커밋(OPS-1). **R2** 무대(호스트 앰버링·크라운·별자리 글로우·발화 글로우·이름칩·감독태그) `7ebf613`·`bd97384`. **R3** 좌도크 세션정보 카드(장르·러닝타임·언어 실데이터·Intl.DisplayNames) `bd97384`(다음순서 큐 defer). **R4** 우도크 카드 스택 `e442e61`: 방분위기=리액션 활기 **실집계 라이트**(활성 리액션 수+발화, 가짜 아님)+발화 파형(장식) / 사운드보드=slots 2×3→sendReaction 재사용 / RightPanel **도구탭 전 보존**(chat·dub·vgen·notes·host — 주인님 콜=카드스택+도구탭). 디렉터노트 배선(ROOM-17, chat 채널 message_type=note·author=participant 위조차단) `7ebf613`. **defer(후속):** 초대하기 상단 승격·라이브 타이머 완전 재배치·하단 스텁 배선(헤드폰/녹음/아바타)·다음순서 큐·채팅 오버레이 버블·영상 스크러버·mood 백엔드 집계·사운드보드 SFX. 게이트 tsc0·lint0·test120/120·docs:check PASS. 배포=프론트 CF Pages만(엔진 무변경).

> **무대 씬 후속 (2026-07-10, Fable — `bd97384`~`2a0a6fc`):** 메인뷰 확대(센터 그리드 1.9fr) → **무대 씬이 센터 칼럼을 꽉 채움**(고정 4/3 aspect 제거·`flex-1 h-full`·place-items stretch → 검은 여백 제거). **무대 씬 배경 배선(엔진 기존재 확인)**: 방장 선택 `background_url`(HostConsole 관리탭 `STAGE_BACKGROUNDS`→`set-room-background` Edge `/scenes/` prefix 검증→`bg_change` 브로드캐스트→전원) 이 **무대 전체 배경 단일 렌더**(opacity-90), 센터 `MainView`는 투명(공유영상 자리)—이중 렌더 제거. 신규 자산 `public/scenes/room-stage/campfire-forest.webp`(주인님 제공·모닥불 테마 정합, WebP 120KB). **빈 아바타 슬롯 숨김**(씬 주역화) + **별자리 직선 연결선 제거**(→ §6.1 별자리 규칙 **폐기**: 동적 아바타 위치 추종이라 정적 생성 불가·씬이 분위기 대체, 원하면 곡선 글로우 코드로만 재작성 가능). 부수: 공유링크 라우트 버그 `/room/`→`/rooms/` 정수정(`b179e72`, 유일 호출처). 배포·인증 실렌더 검증 완료(테스트 방 배경 강제→MainView 씬 확인).

> **이모트 기능화 as-built (2026-07-10, Opus→Fable — `a656783`·`0e907fe`·`0659c05`·`daeafa8`·`83c965a`):** 하단바 스텁 실배선(🎧→믹서·🎭→PiP 배우전용·⏺→'준비중' — §2 "하단 스텁 배선" defer 부분 해소) · 신규 방 기본 배경=모닥불(create-room, **Edge 재배포 대기**) · 우도크 재분배 — 방분위기+사운드보드를 `EmoteConsoleCard` 로 통합(로드아웃 전 슬롯 그리드 + ✏️`EmoteLoadoutPicker`), MoodMeterCard·SoundboardCard 삭제 · **옐로 Lottie 비주얼 레이어**(기본 8종 자산 + `EmoteGlyph` 단일 렌더러, 인룸 E2E 7/7 — 상세 `contracts/ReactionWheel.md §비주얼 레이어`). SFX 오디오·mood 백엔드는 §4 defer 유지. **미배포**(프론트 CF·create-room Edge — `status/HANDOFF-EMOTE-LOTTIE-2026-07.md`).

## 4. defer (컨셉엔 있으나 이번 MVP 밖 — 백엔드 신규, 후속 트랙)

분위기 지수 산출(mood 백엔드) · 사운드보드 SFX 오디오 · 룸 녹음 · 다음순서 큐 로직 · 카메라/헤드폰 룸레벨 토글 · 채팅 오버레이 버블(ChatOverlay §7) · 영상 스크러버 · 별자리 글로우 풀 애니. → 각각 **시각 자리만** 잡고 배선/로직은 후속. 무리한 10기능 동시 신규 = 미니멀리즘 위반, 금지.

## 5. 계약 개정 (구현 시 동반)

- `contracts/RoomView.md` — 2열 → 3존 셸, 상단바/하단바 존, 도크 구성 반영. 기존 §G-261(모드 배너)·PiP 등은 유지.
- `contracts/StageLayout.md` — 3×3 그리드 → 원형 3쌍 배치 + 호스트링/발화자/트래킹/별자리 시각 규칙 확정(현재 "PENDING 좌표"를 실좌표로).
- `DESIGN-DIRECTION.md §6` — "정밀 와이어프레임 PENDING" 해제, 본 문서 참조로 갱신.

## 6. 검증

`type-check && lint && test`(i18n en/ja 완역·roomQuality류 단위) · `docs:check --strict` · `check:responsive`(360/768/1440) · 2탭 실측(발화자/호스트링 동기). 각 페이즈 끝에 게이트, R 전체 후 배포.
