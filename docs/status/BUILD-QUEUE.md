---
tags: [status]
---

# BUILD QUEUE — 기능 우선순위 (UI/UX 무관)

> 순수 기능 ROI 기준. **하나씩**: 하이쿠 딥스캔(라벨 검증) → 진짜 미빌드면 페이블 플랜 → 오푸스 검토 → 구현 → 게이트.
> **철칙: "partial/DONE 라벨"을 믿지 말고 실파일로 검증한다.** DUB-05·VGEN 둘 다 "partial" 라벨이 틀렸고 코어는 완성돼 있었음.
> 스냅샷 2026-07-05. **SSOT 아님(작업 큐)** — 확정 스펙은 `FEATURE-SPEC.md`·`GAP-MATRIX.md`.

## 현재 진행

- **B Host 컨트롤 슬라이스1~3 (Kick·비번룸·Mute) — 구현+검증 완료. 커밋 대기.** 프론트 게이트 5/5 그린(tsc0·lint·test38·build·docs) · 엣지fn 로컬 `functions serve` 실측 **kick 7/7 + 비번·mute 20/20**. 남은 프로덕션-only 확인: 실 LiveKit removeParticipant·updateParticipant 성공경로(로컬은 DB+토큰게이트가 권위라 실측 통과). 다음: A(Avatar 자산배포·데이터 액션)·C(VGEN 공유재생)·D(6인 실증).
  - **slice1 Kick**: is_disabled_by_host+token_version+1 / removeParticipant / Disconnected(PARTICIPANT_REMOVED) 강퇴화면. 재입장403.
  - **slice2 비번룸**: `set-room-password`(PBKDF2 네이티브·room_secrets·is_locked) + `join-room-with-password`(상수시간 대조) + LobbyPage 🔒 + RoomPage 비번입력 단계. 마이그 `20260705120000_create_room_secrets`. join-public-room 은 `_shared/roomJoin.ts`(joinAsParticipant)로 DRY 리팩터(리팩터 후 정상 실측).
  - **slice3 Mute**: `set-participant-mute`(muted_by_host DB권위 + updateParticipant canPublish 토글) + livekit-token 이 `canPublish = role!=viewer && !muted_by_host`(재연결해도 발행차단 실측) + HostConsole 음소거 버튼 + ParticipantPermissionsChanged 감지 → mutedByHost 배지·셀프해제 차단.

## Slice 1 (Kick) — vetted 플랜 (오푸스 검토 반영, 승인 시 착수)

페이블 플랜을 실코드 대조 검증: 스키마·게이트·헬퍼 실재 확인. 아래는 2개 치명 교정 반영본.

**교정(구현 필수):**
- **C1 id공간**: kick 입력 = `target_identity`(=auth uid, 클라가 가진 유일 식별자), **NOT** room_participants.id. 서버가 identity→users.id 매핑해 room_participants 갱신 + `removeParticipant(identity)`. (livekit-token이 `identity:user.id`로 발급)
- **C2 audit_logs 부재**: 테이블 없음 → slice1 감사로그 제거(후속).
- **I1 host 게이트**: HostConsole은 `mySlotIndex===0`(기존 isHost)로 표시. roomStore.hostId 미채움. 서버가 `rooms.host_id`로 진짜 검증.
- **I2**: RoomParticipant 필드 추가 안 함(YAGNI) — identity+name 사용.
- **I3**: 통합/E2E는 기존 `.mjs` 하네스(supabase functions serve, `supabase-slice-verify`). test:e2e/Playwright 없음.
- **재사용**: HostConsole = 새 RightPanel의 host-only 탭.

**edge fn `kick-participant`**: `{room_id, target_identity}` → `getAppUser`→host검증(caller.userId===rooms.host_id)→identity→users.id→`room_participants` UPDATE(is_disabled_by_host=true·token_version+1·token_revoked_at=now)→`RoomServiceClient.removeParticipant(room_id, target_identity)`(실패해도 DB커밋 유지)→200. 비호스트 403·미참가자 404. 재입장 차단=livekit-token `is_disabled_by_host` 게이트(이미 존재)·즉시 절단=removeParticipant.

**파일**: 신규 `functions/kick-participant/index.ts`·`functions/_shared/livekit.ts`(RoomServiceClient 팩토리)·`components/HostConsole.tsx`·`tests/integration/kick-participant.mjs` / 수정 `hooks/useLiveKitRoom.ts`(room-authority reliable 채널 송수신)·RightPanel tabs(host-only 탭)·`i18n/locales/ko.ts`. 리스크: livekit-server-sdk RoomServiceClient Deno 번들(serve 검증) → 실패 시 Twirp REST 폴백.

**슬라이스2(비번)·3(mute)**: room-authority 채널·HostConsole 셸 재활용. 비번=`set-room-password`(bcrypt→room_secrets)+`join-room-with-password`+LobbyPage 🔒. mute=`set-participant-mute`+`mutePublishedTrack`+카운트다운 배지.

## 완료 확인 (스캔 정정 — 코어 이미 완성)

- **DUB-05 합성** ✅ (오푸스 실파일 확인) — ffmpeg.wasm 믹싱·Compositor 전 흐름·edge fn 4·dub_outputs. 원본 재더빙 동작. 잔여=스템캐시·오디오전용·긴클립async·loudness·공유링크·아바타(전부 최적화/P2).
- **VGEN 코어** ✅ (하이쿠 딥스캔) — 백엔드 탄탄: trigger-vgen(검증·게이트·검열·dedup·원자적 크레딧차감·fal async·환불)·vgen-webhook(ED25519·멱등·R2·환불)·pg_cron 타임아웃 스위퍼·크레딧 RPC. **단일 사용자 프롬프트→생성→재생 E2E 작동.** 잔여 기능=VGEN-04 공유재생(main-view, 표면 미존재)·15s 해제·진행률%. 나머지 60%(협업편집·레퍼런스이미지·비율·이의신청·더빙오버레이·프롬프트정제)=slice1b/2 defer.
- **Avatar 선택 (코드+데이터)** ✅ — SettingsPage 선택UI+저장·avatars.ts(manifest+resolveavatar+isValidAvatarUrl)·userStore.setMyAvatar·렌더측 fetchRoomMembers→RemoteAvatar까지 E2E. **데이터 블로커 해제**: 2026-07-05 기준 Storage 라이브 5종(akane·aria·mimi·ruby·yuki), 전부 실렌더 검증(deploy-avatar.mjs+avatar-deploy 스킬). 매니페스트 동적로드라 프론트 재배포 0. 잔여=라이브 아바타변경 broadcast(P2)·추가 캐릭터 온보딩(같은 스크립트).

## 큐 — 진짜 미빌드 프론티어 (라벨 미검증, 착수 시 하이쿠 딥스캔)

| # | 만들 것 | 기능 가치 | 상태 | 주요 파일 / 계약 |
|---|---|---|---|---|
| A | **Avatar 선택 실배선** | VTuber 플랫폼인데 전원 기본 아리아 — 고르고 저장·타인 반영 | SettingsPage 아바타 stub·데이터경로 존재 → 작음·코어 | `pages/SettingsPage.tsx`·`lib/avatars.ts`·`userStore`·`contracts/ModelSelector.md` |
| B | **Host 컨트롤 (kick·mute·비번룸)** | 다인 세션 운영·모더레이션 | **edge fn 자체 없음**·계약만 → greenfield | `contracts/HostConsole.md`·`state-machines/HostAuthority.md`·신규 fn·`lib/rooms.ts` |
| C | **VGEN 공유 재생 마감** | 생성 영상 전원 관람(main-view broadcast)+15s+진행률% | 코어 done·공유표면(stageStore·MainView) 미존재 | `features/vgen/*`·신규 stageStore/MainView·room-authority DataChannel·`contracts/VgenPanel.md` |
| D | **6인 무대 실증·확장** | 6인 30분 안정(Phase 3) | 2인만 검증 | `features/stage/*`·`useLiveKitRoom`·`contracts/StageLayout.md` |
| E | **Lobby 기능 (검색·비번입장·Realtime)** | 방 발견·접근 | **완료** (비번입장 기구현·검색 클라필터·Realtime broadcast 트리거) | `pages/LobbyPage.tsx`·`migrations/20260705130000`·`contracts/LobbyPage.md` |

## 진행 로그

- 2026-07-05: 큐 생성 → #1 DUB-05 하이쿠+오푸스 확인 결과 **완성** → VGEN 승격 → VGEN도 하이쿠 딥스캔 결과 **코어 완성**(백엔드 탄탄, 잔여는 공유재생/slice1b/2). 큐를 "진짜 미빌드 프론티어"(Avatar·Host·VGEN공유·6인·Lobby)로 재편. 다음 타깃 결정 대기.
- 2026-07-05: 사용자 부재 → 자율로 Avatar(A) 하이쿠 딥스캔 → **코드 E2E 완성, 블로커는 데이터(아리아 1개만 배포)**. 3/3 스캔이 "코드 완성" → 메타결론: 앱은 라벨보다 훨씬 완성. code-greenfield 후보는 Host(kick/mute/비번 edge fn 부재)뿐 → Host(B) 하이쿠 딥스캔 착수(구현/커밋 승인 대기).
- 2026-07-05: Host greenfield 확인(스키마·게이트 인프라는 존재) → 페이블이 Kick 슬라이스 플랜 → **오푸스 검토로 2개 치명 교정(C1 id공간=target_identity·C2 audit_logs 부재)** + 4개 중요 교정. vetted 플랜 확정, 구현/커밋 승인 대기. 배턴(하이쿠→페이블→오푸스) 완주.
- 2026-07-05: 사용자 "킥 ㄱ" → Kick slice1 **구현**(엣지fn kick-participant·_shared/livekit.ts·HostConsole·useLiveKitRoom onKicked(PARTICIPANT_REMOVED)·RoomPage host탭+강퇴화면·lib/rooms·i18n·통합테스트) + **검증**(프론트 게이트 5/5·로컬 functions serve 7/7). ponytail 단순화: room-authority 채널 대신 LiveKit 네이티브 Disconnected 사유 사용. 커밋 대기.
- 2026-07-05: **프로덕션 배포**(사용자 "배포 ㄱ"). `db push --include-all`: 마이그 5개(vgen_tables·credits·credit_rpcs·vgen_cron·room_secrets) 적용 — dry-run 선확인, cron은 DO/예외로 pg_cron 없어도 안전. `functions deploy` 9개(kick·set-room-password·join-room-with-password·set-participant-mute·join-public-room·livekit-token·trigger-vgen·get-vgen-url + vgen-webhook `--no-verify-jwt`). 실측: 프로덕션 테이블 4 OK(credits rows=38 백필)·함수 401(배포됨). 시크릿(FAL_KEY·R2_*·LIVEKIT_*) 기설정. **잔여 게이트**: ①`app_config.VGEN_ENABLED=false`(생성 막힘 — 켜면 유료 fal 과금, 승인 필요) ②프론트(CF Pages) 미배포라 Host 컨트롤 UI·VGEN 공유재생 UI가 라이브 앱엔 아직 없음(VGEN 공유재생은 미커밋). ⇒ Host 컨트롤은 서버측 작동, 앱 노출은 프론트 배포 후.
- 2026-07-05: **VGEN 공유재생 라이브 2탭 E2E 통과**(사용자 제공 기존 fal 영상 재활용 — 유료 생성·VGEN_ENABLED flip 0). 방식: 기존 `grid_test_480p_5s.mp4`(9:16·5s)를 짧게 쓰고 지운 프리사인 함수(`tmp-presign`, vgen-test/ 프리픽스·인증 게이트)로 프로덕션 R2 업로드 → `vgen_jobs` done 시드 → 로컬 vite(프로덕션 연결)+헤드리스 2탭(host·guest). 결과 **실질 10/10**(유일 FAIL은 MediaPipe INFO를 console.error로 뱉는 오탐): 호스트 방송→게스트 get-vgen-url 200(자기 서명URL)→**양쪽 MainView R2 영상 재생**(스크린샷 육안: 게스트=영상만, 호스트=영상+공유중지 버튼)→종료 후 자동해제 양쪽. get-vgen-url `members_only` 게이트로 비호스트 멤버 접근 실증. **정리**: 프리사인 함수 삭제·시드 room 삭제(cascade, vgen_jobs 0)·playwright 제거. 잔존(무해): R2 orphan `vgen-test/grid_test_480p_5s.mp4`·테스트계정 e2e-vshare-host/guest. **코드는 여전히 미커밋·프론트(CF Pages) 미배포.**
- 2026-07-05: **VGEN 공유재생(VGEN-04) 구현** — stageStore(신규)·MainView(신규, Stage 센터셀)·useLiveKitRoom `room-authority` 토픽 송수신(신규)·RoomPage 배선(handleRoomAuthority·shareVgen·stopShareVgen)·VgenStatusTab "방에 공유" 버튼·i18n. 설계: 호스트가 **jobId만 방송**→각 참가자가 get-vgen-url(멤버십·visibility=members_only 게이트)로 **자기 서명URL 독립 취득**→센터 재생, onEnded 자기정리(15s 타이머 불요). script-cue 브로드캐스트 패턴 재사용. 프론트 게이트 5/5(tsc0·lint·test38·build·docs). **미커밋.** 검증수위: DataChannel 왕복은 B3에서 2탭 실측된 것과 동일 메커니즘(새 토픽 문자열만)·get-vgen-url 게이트는 실코드 확인. **라이브 E2E는 인프라 블록**(아래).
- 2026-07-05: **배포 갭 발견**(`supabase migration list`): 프로덕션 미적용 = VGEN 백엔드 4마이그(20260704120000~120003: vgen_jobs·credits·rpcs·cron) + slice2 room_secrets(20260705120000). 엣지fn(kick·mute·set-room-password·join-room-with-password·set-participant-mute·vgen 함수들)도 미배포. ⇒ **VGEN은 프로덕션에서 아직 안 돎**(테이블 부재), 커밋된 Host 비번룸도 배포 전엔 비활성. VGEN 공유재생 라이브 검증·비번룸 라이브는 이 배포(마이그+함수, 승인 필요 outward)가 선행돼야 함. 코드는 로컬 정합·게이트 통과 상태.
- 2026-07-05: Host 3슬라이스 커밋(fcd8a30). 아바타 배포: 미미(신규·38parts)+유키(07-03 rig로 갱신·44parts) Vtube→프로덕션 Storage. deploy-avatar.mjs(실존 확인 — RTK가 ls 뭉갬)+매니페스트 안전병합(akane·aria·mimi·ruby·yuki 5종). 낭비방지로 유키는 generated_at 비교(배포본 06-30 < 로컬 07-03)해 재배포 결정. 렌더검증 성역: same-origin 스테이징+헤드리스 각 6/6 + **스크린샷 육안 확인**(미미 고양이귀·유키 토끼귀, parts조립·윙크·입벌림 정상). 레포 무변경(Storage-only). 다음: VGEN 공유재생(item C).
- 2026-07-05: 페이블 독립 코드리뷰(다른 상위모델 교차검증) → 오푸스가 발견마다 실코드 대조 판정. **실수정 2건**: (1) set-room-password 잠금해제 재정렬(is_locked=false 먼저 — 중간실패 시 영구 입장불가 방 방지), (2) livekit-server-sdk `@2` 버전 핀(공급망 결정성). **오탐 2**: 비번검증은 fail-closed(우회 불가), HostConsole mute Set은 await 성공 후 갱신(롤백 불필요). **defer 1**: current_participants 카운트 race는 기존버그·표시용·정원게이트는 실행수 기준이라 안전(슬라이스 밖). **ceiling 1**: mute 시 LiveKit 실패하면 실session은 재연결 전까지 미음소거(DB권위+토큰게이트가 재연결 시 강제 — kick과 동형). **SDK updateParticipant 시그니처 실물 검증**(2.16.0 옵션객체 오버로드 일치). 재검증 로컬 serve **11/11**.
- 2026-07-06: 사용자 "6인 성능측정·안정 하네스" → **Phase 3 성능측정 실행**(throwaway: dev서버+playwright-core, `page.evaluate`서 `/src/lib/pixi/rig` 직접 import해 N개 RigAvatar 마운트·구동, 새 커밋코드 0). 헤드리스 SwiftShader·size120·4s 구동 실측: **N=1** WebGL컨텍스트3·heap Δ3.1MB·fps60 / **N=3** 컨텍스트5·Δ4.3MB·fps57 / **N=6** 컨텍스트8·**heap Δ8.5MB**·**fps28**. **판정: Phase 5(풀링) defer.** ①메모리 우려 debunked — 6아바타 heap Δ 8.5MB(1GB OOM 아님), 컨텍스트 8<브라우저 ~16캡(context-loss 없음). ②fps 60→28(1→6)은 **SwiftShader(SW렌더)+60Hz 전체 over-drive**(실사용은 원격 ~20Hz)라 비관적 프록시 — 실 GPU+실 20Hz면 개선. **⇒ 텍스처/에셋 공유는 실 저사양 기기서 <30fps 확인될 때만**(현재 근거 없음). Phase 4(6탭 30분 안정)는 6계정+prod LiveKit+prod 테스트데이터 생성이 필요한 무거운 E2E라 별도 실행(30분은 배포 후). 정리: perf 하네스·playwright·dev서버 제거.
- 2026-07-06: 사용자 "페이블로 6인 플랜 ㄱ" → 하이쿠 딥스캔 + 페이블 플랜 + **오푸스 실코드 검토**(페이블 교정: slot_index 이미 클라에 있음(list-room-members·RoomPage:162)→엔드포인트 신설 불필요 / SwiftShader≠실GPU라 측정법 교정 / borrow-pool 모델 오류→텍스처공유 / 커밋Playwright→throwaway / 일정추정 삭제) → 플랜모드 승인 → **Phase 1·2 구현·검증**. **Phase 1 slot_index 절대좌석**: `stageLayout.seatParticipants`(순수함수·길이 slotCount 배열·slot 미상/충돌은 최저 빈슬롯 임시) + Stage.tsx identity정렬 폐기 + RoomPage `memberSlots`(기존 fetchRoomMembers 재사용, authId 키) + `slotOf` 콜백. 유닛 6케이스(리플로우 없음·2인 회귀·충돌·정원상한). **Phase 2 참가자별 연결품질**: RoomParticipant.connectionQuality + mapParticipant 매핑 + useLiveKitRoom `ConnectionQualityChanged`→refresh + HostConsole 점배지(🟢🟡🔴❌·행당 1개, UI최소). 게이트 tsc0·lint·test44·build·docs 통과. 미커밋. **남은 것: Phase 3(6-avatar 성능측정 throwaway)→Phase 5(측정 FAIL시 텍스처공유), Phase 4(6탭 30분 안정 harness).** 이들은 dev서버+LiveKit+다계정 필요(별도).
- 2026-07-06: 사용자 "refine go"(하이쿠조사→플랜모드→구현) → **refine-vgen-prompt 구현**(프롬프트 LLM 정제). 하이쿠가 Seedance 2.0 공식 프롬프트 규칙 웹조사(fal.ai/ByteDance: Subject→Action→Camera→Scene→Style·50~150단어·동사우선·표준카메라용어·"fast"금지·negative미지원→인라인"no X"·@Image태그·"Shot N:/cut to"). 플랜모드 승인(사용자 "기능만·UI최소화"로 1차 반려→프론트 버튼1개로 축소). **신규 Edge `refine-vgen-prompt`**: 호스트·무과금·무DB, 입력 `{room_id,rough_prompt,reference_asset_ids?,target_duration_sec?}`→`{refined_prompt}`, 시스템프롬프트에 공식규칙 인코딩(refs>0 조건부 @Image·duration 조건부 멀티샷힌트), LLM=gpt-4o-mini(env로 NIM 교체·키 서버보관). 모더레이션 refine단 생략(trigger-vgen enforcement). **프론트 최소**: VgenPromptPanel 버튼1개("✨ 다듬기"→textarea 교체)·기존 에러영역 재사용·lib/vgen 래퍼·i18n 3키. 게이트 tsc0·lint·test·deno check·build·docs 통과. **실 LLM 검증(성역·함정17)**: 개떡 한글 3케이스 실호출 → 영어 Seedance 구조 프롬프트·slow push-in/tracks alongside·"No text,no watermark"·refs3→@Image1·10s대사 큰따옴표 전부 실동작 육안확인. Edge auth/serve는 deno check+trigger-vgen 동형패턴(full serve-E2E는 boilerplate라 생략). 미커밋. **다음 VGEN=reference-to-video 전환(아바타→4K 시트 파이프라인, refine @Image 실사용).**
- 2026-07-06: 사용자 "ㄱ"(해상도) → **VGEN 해상도 사용자선택 + 해상도가중 크레딧** 구현(slice1b 결정 일부). fal 스키마·단가 먼저 실확정(WebFetch): `bytedance/seedance-2.0/reference-to-video` resolution enum=480p·720p·1080p·4k, aspect_ratio 9:16 지원, 단가 720p·비디오없음 std$0.3024/fast$0.2419·해상도 상승(1080p 정확배수 미표기). 구현: **백엔드** trigger-vgen이 body.resolution 검증(허용집합·기본720p)·`cost=ceil(duration×RES_CREDIT_WEIGHT[res])`(480p 0.5·720p 1·1080p 2.5)·promptHash에 resolution 추가(해상도별 dedup 분리)·estimated_cost_usd 해상도별·fal input resolution 동적화(하드코딩 "720p" 제거). **프론트** lib/fal(가중/단가 맵·creditCost(dur,res)·estimateUsd(dur,res))·vgen.ts·vgenStore·VgenPromptPanel(해상도 셀렉터 480/720/1080·실시간 크레딧표시)·i18n. 게이트 tsc0·lint·test·build·deno check·docs 통과. 산식실측 720p보존(5→5·10→10)·1080p 10s=25cr·480p 5s=3cr. **⚠️ 크레딧 가중은 보수적 추정 — VGEN_ENABLED=true 전 fal 2.0 라이브 단가로 재검증(1080p/4k 정확배수). 4k·9:16 aspect·업스케일(사용자 테스트전용)은 미포함.** 미커밋. **다음 VGEN 슬라이스=reference-to-video 전환(아바타→4K 캐릭터시트 파이프라인 선행) + refine-vgen-prompt 신설(현재 문서만·프롬프트 raw 직행).**
- 2026-07-06: 사용자 "조인 레이스" → 페이블 ③ **근본 수정**. 원인=`roomJoin.ts` SELECT→INSERT→UPDATE 3-step 비원자성(동시 조인이 stale count 공유 → current_participants 저평가·슬롯 충돌). 수정=RPC `join_room_as_participant`(마이그 `20260706120000_atomic_room_join`)가 rooms 행을 `FOR UPDATE` 로 잠가 방별 조인 직렬화 → 활성수·슬롯·정원 모두 락 안에서 정확. roomJoin.ts 는 3콜→1콜(status→HTTP 매핑만), 두 호출처(join-public-room·join-room-with-password)는 max_participants 인자·select 제거(RPC가 락 안에서 읽음). SECURITY: p_user_id 파라미터라 클라 노출 시 임의 방 삽입 가능 → PUBLIC/anon/authenticated EXECUTE revoke, service_role 만 grant(실측 proacl 확인). **동시성 실측**: max6 방에 10명 동시 조인 5회 반복 = 매번 정확히 6 joined·4 full·슬롯[0-5] 유니크·current_participants=6, 멱등(동일유저 2회 동시)=[joined,rejoined]·1행, ALL PASS. 검증 중 내 RPC 실버그(slot_index OUT/컬럼 ambiguous) 테스트가 잡아 수정. 엣지층 `deno check` 클린(roomJoin·join-public-room exit0; join-room-with-password 의 password.ts 타입경고는 기존 slice2 코드·프로덕션 검증됨·무관). 프론트 무변경.
- 2026-07-06: 사용자 "페이블 리뷰" → 로비 교차모델 리뷰. 페이블 3건, 오푸스가 실코드/로컬 실측 대조. **실수정 2**: (①MAJOR **realtime.send 예외 결합** — AFTER 트리거 미처리 예외가 방 생성/입장/퇴장 트랜잭션을 롤백 → 핵심 쓰기가 Realtime 건강에 볼모. BEGIN..EXCEPTION 로 fail-open. **로컬 양방향 실측**: 핸들러 있으면 send raise(1/0)해도 방 INSERT 커밋, 없으면 롤백. ②MINOR **max_participants 감시 누락** — 로비 full 계산이 이 컬럼에 의존 → 감시절에 추가, 실발화 확인 delta=4). **defer 1**: ③roomJoin.ts `current_participants` stale-read 레이스 = 기존버그·범위밖·정원게이트는 실행수 기준이라 안전·표시용(slice2에서 이미 문서화). 나머지 각도(search_path·definer·컬럼누출·React 생명주기·확장성)=clean. 마이그 파일 갱신·로컬 재적용, 프론트 무변경(게이트 재실행 불요).
- 2026-07-05: 사용자 "로비 ㄱ" → **로비 마감(item E) 구현+검증**. 실파일 검증으로 스코프 확정: **비번입장은 이미 완결**(LobbyPage 클릭 → `join-public-room:35` 403 "Room is locked" → RoomPage `password` 단계 → `join-room-with-password`, 보안 정상). 남은 실빌드=**Realtime 자동갱신** 하나. 설계결정: postgres_changes(계약 §실시간갱신방식 지시)는 rooms RLS(참가자 전용)에 막혀 로비에 안 옴 → RLS를 넓히면 뷰가 숨긴 컬럼(host_id·authority_state_json·background_key) 노출(보안 후퇴)이라 반려 → **broadcast 트리거**(마이그 `20260705130000`: rooms 변경 시 민감정보 0 nudge 를 public `lobby` 채널로 `realtime.send(private=false)`, 클라 debounce 400ms 후 뷰 재조회). 추가: **검색 클라필터**(제목·호스트명). 검증: 게이트 5/5(tsc0·lint·test38·build·docs) + **트랜잭션 트리거 실발화**(감시컬럼 INSERT/UPDATE/DELETE 3·무관컬럼 0) + **로컬 실클라 브로드캐스트 배달 PASS**(anon 구독 → service_role rooms 변경 → 수신, playwright 불요 node 검증 후 삭제). 로컬 오염 0. **미커밋·프론트 미배포·프로덕션 마이그 미적용**(1개: 20260705130000). i18n ko 3키(search·noMatch). 장르 태그는 create-room 백엔드 배선 필요라 defer.
- 2026-07-05: 사용자 "slice2·mute ㄱ" → 비번룸(slice2)+Mute(slice3) **구현+검증**. 신규 마이그 1(room_secrets)·엣지fn 3(set-room-password·join-room-with-password·set-participant-mute)·공유헬퍼 2(password.ts PBKDF2·roomJoin.ts) / 수정: livekit-token(muted_by_host→canPublish)·join-public-room(DRY)·lib/rooms·roomStore(mutedByHost)·useLiveKitRoom(권한변경 감지)·HostConsole(비번·음소거)·RoomPage(비번단계·배지)·LobbyPage(🔒)·i18n. 설계결정: mute 강제는 mutePublishedTrack(sid 필요) 대신 updateParticipant(canPublish) — 재발행까지 차단·코드 더 적음. 비번은 bcrypt 의존 대신 네이티브 PBKDF2(보안 성역 유지). 프론트 게이트 5/5 + 로컬 functions serve **20/20**(비번 9·mute 7·DRY 1·unlock 3). 통합테스트 아티팩트 2개 정규위치 추가. 커밋 대기.
