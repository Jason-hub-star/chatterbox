---
tags: [audit]
---

<!--
  dogfood-audit(2026-07-06) 마스터 산출물 — 6 페르소나 서브에이전트 도그푸딩 + 메인 검증.
  이 문서 = 발견 SSOT + 최우선 구현 백로그(체크리스트). UX 상세는 design/UX-GAPS-AND-PATTERNS.md.
  스킬: .claude/skills/dogfood-audit. 재현: "플랫폼 감사해줘" / "도그푸딩".
-->

# 도그푸딩 감사 2026-07 — 발견 & 최우선 구현 백로그

> **BLUF:** 아키텍처는 견고(인증·RLS·크레딧 원자성·웹훅 서명·a11y 시맨틱이 실측 반증됨). 진짜 갭은 **레이트리밋 부재 계열 보안 6건**과 **1초-피드백·모바일/뷰어·다국어 UX 공백**. 방법 = 하이쿠 정찰 → Opus 6페르소나 워커 → 메인 직접 검증(인용라인 대조). UX 상세·프리미티브 설계는 [design/UX-GAPS-AND-PATTERNS.md](./design/UX-GAPS-AND-PATTERNS.md).

---

## §0. 최우선 구현 백로그 (하나씩 — 체크로 추적)

> **2트랙 운영(트랙분리):** **A = 로직·보안·seam**(지금, `/다음` 대상) · **B = UIUX 프레젠테이션**(나중, 스펙 대비 1회 패스). `/다음`(구 /backlog)은 **트랙 A만** 민다. B는 A가 심어둔 seam 위에 스타일만 얹는다 → "나중 연결"이 "나중 재작성"이 되지 않게.

### 트랙 A — 로직·보안·seam (지금 · `/다음` 대상)

**A-P0 (보안 실침해)** — 재감사 2026-07-07 신규 2건(SEC-7·SEC-8): **수정+프로드 배포 완료 · 라이브 구멍 차단 실측.**
- [x] **SEC-7** (High·재감사 신규) **크레딧 RPC 클라 직접호출 노출** — `deduct_credit`·`refund_credit`이 `revoke ... from public`만 해서 **anon/authenticated 는 여전히 EXECUTE 가능**(Supabase `alter default privileges ... grant execute on functions to anon, authenticated` 가 명시적 부여 → public revoke로 안 빠짐). 익스플로잇: 앱 anon 키로 `rpc/refund_credit(<본인 완료잡>)` → 소비한 유료 생성 무한 무료화 / `rpc/deduct_credit(<피해자>, 9999, ...)` → 타인 크레딧 고갈. **정수정:** 새 마이그 `20260707120000_lock_rpc_execute.sql` — `revoke ... from public, anon, authenticated`(= `join_room_as_participant` 패턴). service_role(Edge)은 default-priv 유지. **실측:** 격리 pg17.6.1.127(BEFORE auth=t → AFTER anon/auth=f·svc=t) + **프로드 배포 전 `deduct_credit`·`refund_credit` anon=t·auth=t(라이브 노출 확정) → 배포 후 anon=f·auth=f·svc=t(차단 확정)**. ✅ **프로드 배포 완료**(`supabase db push`, 2026-07-07). 마이그 파일 미커밋(커밋 승인 대기).
- [x] **SEC-8** (Med·재감사 신규) **레이트리밋 RPC 노출** — `check_rate_limit`(마이그 `20260706130000`)에 **revoke 전무** → anon/authenticated EXECUTE 가능. 익스플로잇: `rpc/check_rate_limit('vgen:<피해자>', …)` 반복호출로 피해자 버킷 선증가 → 정상요청 429(vgen/refine/transcribe/translate/separate DoS + SEC-1 비번조인 버킷 잠금 → SEC-1/4/6 무력화). **정수정:** SEC-7과 같은 마이그에 `revoke ... from public, anon, authenticated` 포함. **실측:** rate_limit 마이그는 배포 전 프로드에 미존재(SEC-8 미노출 상태였음) → 이번 push로 `check_rate_limit` 생성과 **동시에 잠금**(프로드 배포 후 anon=f·auth=f·svc=t). ✅ **프로드 배포 완료**(동일 push).
- [x] **SEC-1** 잠금방 비번 **브루트포스 레이트리밋** — `rate_limit_counters` + `check_rate_limit` RPC(마이그 `20260706130000`) + `join-room-with-password` 적용(5회/5분→429, **정답 시 카운터 리셋** = 실패만 누적·정상유저 오탐 제거). **로직 실측**(supabase pg17.6: t×5→f→윈도리셋→키격리→RLS + 리셋-온-성공: 3실패→delete→fresh count1). ⏳ 배포·통합 실측 대기. SEC-4가 이 프리미티브 재사용.
- [x] **SEC-2** 스토리지 **경로 조작 차단** — 공용 `isSafeObjectKey`(supa.ts, `<room>/<subdir>/<안전파일명>` 정확매칭) + `submit-dub-track`·`submit-dub-output`·`create-dub-session`(sources/vgen) 적용. **실측**(deno check clean · 헬퍼 11/11: 정상키 통과·`../`·여분슬래시·타방·선행점·비문자열 전부 차단). ⏳ 배포 대기(앱 레이어에서 이미 거부 → R2 정규화 동작과 무관하게 견고, R2 실테스트는 배포 후 확인).
- [x] **SEC-3** SSRF 차단 — `isOwnR2RefUrl`(r2.ts): image_urls를 우리 R2 `vgen-refs/<room>/` presigned URL로만 제한(오리진+경로 allowlist·`..`거부). **실측**(deno check clean · 11/11: 정상 통과·메타데이터/사설IP/외부/서브도메인트릭/타방/타버킷/traversal/http 전부 차단). 참조자산 테이블 없어 asset_id 대신 오리진 allowlist 채택(프론트 무변경). ⏳ 배포 대기.

**A-P1 (비용·정합 + UX seam)**
- [x] **SEC-4** 무제한 비용 API 캡 — `check_rate_limit`(SEC-1 프리미티브 재사용)를 refine(50)·translate(30)·transcribe(30)·separate(20)/일 캡으로 4함수 적용(호스트 체크 직후·외부호출 직전, 무과금 ko-skip 이후 계수). **실측**(deno check clean ×4; RPC 로직은 SEC-1서 실측). ⏳ 배포 대기.
- [x] **SEC-5** 대본 큐 **서버 릴레이** — 새 Edge `advance-script-cue`(host 검증→broadcast) + receiver 스푸핑방어(participant 있으면 드롭) + sendCue 릴레이 전환 + handleCue self-echo 게이트(호스트 무시). **실측**(type-check·lint·**test 50/50**·deno check clean). ⏳ 배포 대기(함수 deploy **+ 프론트 CF Pages 재배포** 둘 다 있어야 라이브 — 리액션과 동일). 라이브 LiveKit E2E는 배포 후.
- [x] **A-SEAM-1** 피드백 채널 — `toastStore`(큐 채널 + 4s 자동소멸을 store가 소유 → B는 렌더만) + emit API(`toast`/`useToast()`, 컨벤션 §5 정렬). 침묵 실패였던 cue relay `.catch(() => {})`(useLiveKitRoom:313) 하나를 `toast.error(t('room.cueSyncFailed'))`로 배선(SEC-5 서버릴레이 실패 시 호스트가 인지). **실측**(type-check·lint clean·**test 53/53** [+3 toastStore: push·자동소멸·dismiss]·build PASS). 표현(`<ToastHost/>`)은 B. *(P-1 로직 — 초안의 no-op/console 대신 실 큐 채널로 격상 = B가 순수 프레젠테이션)*
- [x] **A-SEAM-2** 진행/ETA 데이터 노출 — VGEN: 순수 `estimateVgenSeconds()`(lib/vgenEta) + `vgenStore.currentJobEtaSec` 노출(생성 시작 시 세팅·종료/리셋 시 null, 경과는 `currentJob.createdAt`로 B가 계산). DUB: phase+progress는 이미 `DubCompositor` 로컬상태로 렌더 위치에 존재 → dubStore 신설은 YAGNI(SCOUT 기록), B가 그 자리에서 바만 얹음. `vgen_jobs.estimated_duration_sec` DB 컬럼은 배포 게이트라 클라 추정으로 대체(ponytail ceiling: fal 큐 ETA가 업그레이드). **실측**(type-check·lint clean·**test 56/56** [+3 vgenEta: 단조·클램프·정수]). 바는 B. *(P-2 로직)*
- [x] **A-SEAM-3** Realtime 구독 훅 — 제네릭 `useRealtimeRow(table, col, value, onChange)`(hooks/, latest-ref 패턴으로 대상 변경 시만 재구독) 신설, DubPanel에 2구독(`dub_sessions` room_id·`dub_tracks` dub_session_id) → 변경 시 신뢰소스 재조회, **수동 새로고침 버튼 제거**. 컬럼명·publication을 마이그(`20260702060001`)로 실대조(둘 다 supabase_realtime 등록). **실측**(type-check·lint clean·**test 56/56**·build PASS). vgen currentJob은 이미 store realtime. ⏳ 라이브 realtime 검증은 CF Pages 재배포 후. *(P-3)*
- [x] **A-FUNC-1** 룸 조인 타임아웃/취소 — 중복 `callFn` 3개(rooms·dub·vgen)를 공유 `lib/edgeFn.ts`로 통합 + **15s 타임아웃**(`EdgeTimeoutError`) + 외부 취소 `signal`(취소 버튼은 B). joinRoom→callFn 이라 RoomPage 조인 무한대기가 15s에 에러로 종료(기존 에러표시에 흡수). **실측**(type-check·lint clean·**test 58/58** [+2 edgeFn: 미응답→15s 타임아웃·외부취소→AbortError]·build PASS). 근본수정=공유함수 한 곳(전역 규칙).
- [x] **A-FUNC-2** 인증 복구 — userStore 3액션(`requestPasswordReset`·`resendVerification`·`updatePassword`, enumeration 방지 boolean) + LoginPage 비번찾기(메일 발송·동일 안내) + RegisterPage PENDING 재전송+60s 쿨다운 + `/reset` 착지 페이지(복구세션 updateUser)+라우트. 비번 강도 규칙은 `lib/authValidation.passwordIssue`로 추출(가입·재설정 공유·근본수정). **실측**(type-check·lint clean·**test 62/62** [+4 authValidation]·build PASS). ⏳ 실 이메일 발송·복구 완료 흐름은 배포+실메일 게이트(supabase.auth 호출은 API 정합·컴파일 확인).

**A-P1b (재감사 델타 2026-07-08 · 예약 하드닝)** — 신규 표면(초대·뷰어·예약·최근사람) 델타 감사 발견. 초대·뷰어 경로는 반증(견고).
- [x] **SEC-RES-1** (Med) 예약 생성 rate-limit + 무권한 초대 차단 — `create-reservation`에 `check_rate_limit('reservation:<userId>', 20, 86400)` 일일 캡 추가(SEC-1/4/6 프리미티브 재사용·유효요청 통과 후 계수) + invitee 를 "내 최근 함께한 동료"(공유 room_participants 이력 ∩ 미삭제)로 제한(임의 uuid → 관계 없는 유저 알림 주입 차단). 응답 `notified` 를 실제 발송수(fellowIds)로 정정. 두 수정 모두 `create-reservation` 한 곳. **실측:** deno check clean(RPC 로직은 SEC-1서 격리 postgres 실측·동일 프리미티브). ✅ **프로드 배포 완료**(functions deploy, 2026-07-08 · auth 게이트 401 실측).
- [x] **SEC-RES-2** (Low) `list-recent-people` excludeRoomId 미검증 — 클라 공급 room_id 를 `roomIds.includes(excludeRoomId)`(내 최근 참가 이력)로 게이트 후에만 제외 조회(`list-recent-people`). 정상 사용=내가 있는 방 제외라 무손상, 임의 방은 무시 → 추론 차단(추가 쿼리 0, roomIds 재사용). **실측:** deno check clean. ✅ **프로드 배포 완료**(functions deploy, 2026-07-08 · auth 게이트 401 실측).

**A-P1c (재감사 델타 2026-07-10 · 룸 리디자인+이모트+승격경로+아바타포지 표면)** — 3칼럼 셸·이모트 기능화·viewer→actor 승격·아바타 포지 신규표면 델타 감사. 승격 사슬·호스트권한·cross-room·studio·avatar RLS 는 반증(견고). **5건 전부 구현·커밋·프로드 배포 완료(2026-07-11 — 함수 4 deploy+CF Pages, 배포판 2탭 E2E 10/10).**
- [x] **SEC-RA-1** (Med) room-authority broadcast **participant 미검증 스푸핑** — 수신핸들러(`useLiveKitRoom.ts:203-210`)가 `if(participant)return` 방어를 script-cue/script-role/reaction 3토픽엔 넣고 **room-authority만 누락**. 악성 클라가 `mode_change`/`bg_change`/`vod_sync` 위조 publish → 전원 UI desync·영상 점프(griefing). **권한상승·유출은 차단**(promoted→livekit-token canPublish 재발급·vgen→getVgenUrl 멤버십·kicked→PARTICIPANT_REMOVED 상태게이트 3중 재검증) → 워커 High를 실임팩트 Med 강등. SEC-5(script-cue 스푸핑) 잔여. 정수정: 서버전용 타입은 `participant===undefined`만 수락 + 클라 호스트타입(vod_sync/vgen_*)은 호스트 identity 대조, 또는 토픽 분리. **구현됨(2026-07-11 실측):** 수신이 서버릴레이(participant=undefined)만 신뢰, 클라 발행은 호스트 클라 타입+호스트 identity 대조만 수락·그 외 드롭(`useLiveKitRoom.ts:211-224`)·게이트 그린(tsc·lint·test 131). ✅ 라이브(2026-07-11 CF Pages 재배포·배포판 2탭 E2E 10/10 콘솔 0). <!-- probe: src/hooks/useLiveKitRoom.ts :: SEC-RA-1 -->
- [x] **SEC-AVJ-1** (Med·Modal 라이브 시 High) create-avatar-job **레이트리밋·크레딧 부재** — `create-avatar-job:39-45` rate-limit 0·credit 차감 0 → 봇 무한 잡→Modal GPU spawn 무한 무과금 비용. 표면 라이브(`/atelier-forge`·CommissionCorner 의상실 배선). IDOR는 `isSafeObjectKey` authId 고정으로 차단됨. 정수정: `check_rate_limit`(SEC-4 패턴) + 향후 credit 연결. **구현됨(2026-07-11 실측):** 레이트리밋 10/시(`create-avatar-job:33-34`) + 크레딧 차감/환불(deduct/refund_avatar_credit·플래그 게이트, 마이그 `20260710170000`)·deno check clean. ✅ 프로드 배포 완료(2026-07-11, db push+deploy v2). <!-- probe: supabase/functions/create-avatar-job/index.ts :: check_rate_limit -->
- [x] **SEC-RXN-1** (Med) send-reaction **서버 레이트리밋 부재** — `send-reaction:7` ponytail 주석대로 클라 5/s 쓰로틀만·서버 캡 0 → 봇 방참가 후 broadcast 폭탄. 멤버십 게이트는 있음. 정수정: `check_rate_limit` room+user 캡. **구현됨(2026-07-11 실측):** `check_rate_limit` 적용(`send-reaction:49`)·deno check clean. ✅ 프로드 배포 완료(2026-07-11, v4). <!-- probe: supabase/functions/send-reaction/index.ts :: check_rate_limit -->
- [x] **SEC-CR-1** (Low) create-room **방생성 레이트리밋 부재** — `create-room:37` 방생성 자체 rate-limit 0(팔로워 notify만 캡). 방 무한생성 griefing·로비 오염. API-SURFACE `account_restrictions.can_create_room` 미구현 설계갭. 정수정: `check_rate_limit`. **구현됨(2026-07-11 실측):** 방 생성 5/시(`create-room:37-38`)·deno check clean. ✅ 프로드 배포 완료(2026-07-11, v9). <!-- probe: supabase/functions/create-room/index.ts :: check_rate_limit -->
- [x] **SEC-CR-2** (Low) create-room **language 미검증** — `create-room:31` 길이·화이트리스트 없이 저장(genre는 화이트리스트). React escape로 현 XSS 아님·저장 오염만. 정수정: genre 동형 화이트리스트. **구현됨(2026-07-11 실측):** `LANGS` ko/en/ja 화이트리스트(`create-room:32-33`). ✅ 프로드 배포 완료(2026-07-11, v9). <!-- probe: supabase/functions/create-room/index.ts :: LANGS -->
- [x] **UX(트랙 B)** — 2026-07-12 G2 재감사: 대부분 후속 커밋들로 이미 해소돼 있었음(stale) — toast 3종(링크복사 `RoomPage.handleShareLink`·로드아웃 저장 `EmoteLoadoutPicker:4`·리액션 실패는 Toast UI 패스에서)·`ReactionOverlay` reduced-motion 가드(:28)·`ReactionWheel` 키보드 탐색(:54-64 화살표/Enter/Esc)·마이크 거부 toast(`useLiveKitRoom:330 micBlocked`) 전부 실측 확인. G2 신규: 혼자 입장 빈 무대 안내 칩(`room.soloInviteHint`→공유 CTA). 잔여 defer: 리액션 진입 경로 안내 카피(발견성 — G7 연출 때 동반). <!-- probe: src/features/room/ChatNotesTab.tsx -->

**A-P1d (재감사 델타 2026-07-14 · dogfood 2차 — 6페르소나 Opus 워커 + 메인 직접검증)** — 보안 견고 재확인(반증 대량: `requireHostRoom` 전 호스트함수·크레딧 원자성/멱등·vgen-webhook ED25519·SSRF fail-closed·업로드 서버측키/`isSafeObjectKey`·상수시간 비번). 신규 실결함 3건.
- [~] **SEC-KICK-1** (P2·Confirmed) **강퇴 우회** — `kick-participant`(:52-59)는 `is_disabled_by_host=true`만 세팅(state 불변)하는데 멤버십 체크가 `state!='left'`만 봄 → 강퇴자가 자기 JWT로 Edge 직접호출해 계속 활동(**모더레이션 우회**, 권한상승 아님·livekit 재입장은 차단됨). **정수정:** 공유 게이트 `requireActiveParticipant`(`_shared/supa.ts`, `is_disabled_by_host is not true` null-안전) 신설. **구현됨(2026-07-14, 부분):** `send-chat`·`send-reaction` 적용(deno check clean + PostgREST 필터 프로드 실측: 문법 수용·의미 정확). **배포+라이브 실증(2026-07-14):** send-chat v3→4·send-reaction v6→7(functions list UPDATED_AT 실측) · 프로드 델타 테스트 = 활성 참가자 send-chat 게이트 통과(502 ≠403) vs **강퇴 참가자 → 403 "Not a participant"**(임시유저 admin 생성·강퇴 전/후, finally 정리). ⚠️ `raise-hand` 구현미정(주인님 명시) → 그 슬라이스서 게이트 반영.
  - **⚠️ Phase 2 발견 — blast radius 21함수**: `is_disabled_by_host` 검사는 kick·livekit-token만, 나머지 **21개 함수가 `neq(state,left)`만 봄**(강퇴자를 활성멤버로 취급). 특히 **데이터접근**(`get-dub-source-url`·`get-dub-output-url`·`get-dub-recordings`·`get-recording-url`·`get-vgen-url`)은 강퇴자가 **방 녹음/더빙/영상 signed URL 계속 취득**(데이터 노출 — 채팅 도배보다 심각). 나머지: 대본큐·스테이지초대/수락·투표·동의·mute·목록. → **SEC-KICK-2**(아래)로 확장 추적.
- [~] **SEC-KICK-2** (P2, 데이터접근분 우선) 강퇴자 활성멤버 취급 형제 호출처 — `isActiveParticipant` 프리미티브(`_shared/supa.ts`, `requireActiveParticipant`가 재사용·getter는 room-ended 미검사로 종료된 방 다운로드 유지)로 치환. **구현됨(2026-07-14):** 데이터접근 5함수 `get-dub-source-url`·`get-dub-output-url`·`get-dub-recordings`·`get-recording-url`(members_only)·`get-vgen-url`(members_only) 적용(deno check clean 7파일). **잔여:** 스테이지/대본(advance-script-cue·sync-script-role·invite-to-stage·accept-stage-invite)·투표(submit-viewer-poll)·동의(record-consent·record-recording-consent)·mute·목록(list-*)·leave-room ~14함수 — 저심각도, 후속. **배포 완료(2026-07-14, 전함수 배포=드리프트 근절):** 데이터접근 5함수 버전 bump 실측(get-dub-source 9→10·output 9→10·recordings 8→9·recording-url 2→3·vgen-url 5→6, UPDATED_AT=오늘) · 라이브 실증 = 비호스트 get-dub-recordings 403(SEC-DUB-1 겸증).
- [x] **SEC-AVJ-2** (P2) **create-avatar-job 레이트리밋 과대** — `check_rate_limit("avatar-job", 10, 3600)`=10/시(240/일, :76) vs vgen 3/일 불균형. 서로 다른 PNG 240장/일 = Modal GPU 대량 소각 가능. **완화 존재**(콘텐츠-해시 디덥=같은그림 0원 + 크레딧 구조 플래그게이트). **정수정:** 공개 전 캡 하향 or `AVATAR_CREDIT_ENABLED` on. **구현됨(2026-07-14):** 시간창→일창 전환 `p_max: 8, p_window_sec: 86400`(240/일→8/일, GPU 소각 상한 30× 하향). 캐시히트는 계수 제외라 distinct 신규만 카운트·에러문구 일일한도로 교체. deno check clean. **배포 완료(2026-07-14):** create-avatar-job v5→6 실측(라이브 캡 실증은 9잡 소진 필요라 생략·코드/배포 확인).
- [x] **SEC-DUB-1** (P3) **get-dub-recordings 호스트 미검증** — 방멤버면 발급(:34-41), 계약(DubCompositor §1)은 호스트 전용. 비호스트 멤버가 동료 더빙 녹음 signed URL 취득(협업자·합성물 공유예정이라 경미). **정수정:** `host_id` 검증 추가. **구현됨(2026-07-14):** 신규 프리미티브 `isRoomHost`(`_shared/supa.ts`·종료방 허용=합성 후처리·`from("rooms")`는 _shared 격리로 edgeHostGuard 무플래그)로 `isActiveParticipant`→호스트 전용 치환. 클라 `fetchDubRecordings`는 호스트 전용 `run()`서만 호출이라 정상흐름 무회귀(취약점=UI 우회 직접호출→403). deno check clean. **배포+라이브 실증(2026-07-14):** get-dub-recordings v8→9 실측 · 프로드 델타 = 비호스트 활성멤버 → **403 "호스트만 내려받을 수 있어요."** vs 호스트 → 200.
- 반증(P0 오판 정정): DUB 유료 compute(`start-dub-transcription`:47,51·`separate-dub-audio`:45,49)는 **호스트전용 + 레이트리밋(30/일·20/일, "SEC-4 비용-DoS 차단")** 실재 → 경제워커 "무제한 P0" 은 grep이 레이트리밋 줄 누락한 **오판**. 잔여는 dub만 per-user 크레딧 미청구(P3 빌링 패리티 — vgen/avatar 패턴 미러 defer).

**UX 델타 2026-07-14 (트랙 B/기능)** — 상세·프리미티브는 `design/UX-GAPS-AND-PATTERNS.md`. **CF Pages 배포 완료(2026-07-14):** 프론트 5항목(VIEWER-BADGE·ONB-1/ATEL-1·STAGE-VIS·MOBILE-1·A11Y-1) chatterbox-7r8.pages.dev 배포 — 번들 시크릿 감사 클린(VITE_ 인라인=URL·anon·oauth만·시크릿 6종 0)·curl 3종 200(root/asset/deep SPA폴백).
- [x] **UX-VIEWER-BADGE** (Blocker) 뷰어 "관전 중" 상태 UI 미표시(`room.watching` 키만 존재·`RoomPage`:179,201 내부만) → RoomJoinGate 통과 후 배지. 역할 전환이 안 보임. **구현됨(2026-07-14):** `RoomTopBar`에 `isViewer` prop + LIVE 옆 배지(👀 아이콘+`room.watching` 텍스트, 색상단독 회피·`role=status`). 뷰어는 하단바 마이크가 숨겨져 자기 상태 확인 경로가 없던 걸 해소. tsc0·lint·i18n 3국어 완비.
- [x] **UX-ATEL-THUMB** (High·이번 세션 기확인) 커미션 아바타 **썸네일 미생성**(`publish-avatar-job.mjs`/Modal 워커가 project.json+parts만 발행·thumb.png 0) + **최신 잡 상대URL 오염**(d249f945=지연벤치 잔여, 스토리지 실존하나 DB result_project_url이 상대경로 → 렌더/저장/썸네일 전부 깨짐). **정수정:** 발행에 thumb 굽기 스텝 + `isValidAvatarUrl` 필터(`avatars.ts` myAvatars) + 오염행 정리(프로드 DB 쓰기·승인). **구현됨(2026-07-14):** `AtelierPage` myAvatars `isValidAvatarUrl` 필터(상대URL 방어) + d249f945 삭제(프로드·착용유저 0 실측) + `publish-avatar-job.mjs` 썸네일 굽기 스텝(120s 타임아웃·best-effort) + 커미션 4건 백필(thumb.png 200 실측). 게이트 tsc0·lint·test147. 잔여: Modal 워커 자동 썸네일(Vtube 레포·소스PNG 축소본).
- [x] **UX-STAGE-VIS** (High) 음소거·손들기·연결상태가 `HostConsole` 탭에만 → 호스트가 무대 보다 놓침. 무대 슬롯 상태 오버레이(색+아이콘, 색상단독 회피). **구현됨(2026-07-14):** `StageSlot`에 `relative` + `Stage`가 점유 슬롯 우상단에 `SlotStatus` 배지(🔇 음소거·🔴/❌ 연결저하, 아이콘+aria-label, 정상은 미표시). 데이터=`mutedIdentities`(서버 진실·전 멤버)+`connectionQuality`(RoomParticipant). **손들기는 스코프 제외(구현미정).** 연결 배지는 LiveKit 이벤트로 라이브; mute 배지는 호스트 본인은 라이브(mute 콜백서 멤버 refetch bump), 비호스트 라이브 전파는 mute broadcast 부재 ceiling(후속).
- [x] **UX-ONB-1** (High) 신규 유저 로비→의상실 다음스텝 CTA·가이드 없음(`LobbyPage`:29,233) → 온보딩 후 1회 CTA. **구현됨(2026-07-14):** `OnboardingGuide` 3스텝 확장(환영→장르→**아바타 CTA**) — genre 다음 "의상실로 가기"([의상실로 가기]=complete+navigate, [건너뛰기]=complete). 온보딩은 intro/genre 유저에게만·1회만 뜨므로 별도 영속 불필요(new key 2·재사용 next/skip). genre skip 도 선택장르 보존(`finish(picked)`).
- [x] **UX-ATEL-1** (High) 아바타 저장 실패 재시도 경로 없음(`AtelierPage`:224-227, alert만) → [다시 입기] 버튼. **구현됨(2026-07-14):** 실패 시 `trying` 유지로 [입기] 버튼이 남던 걸 활용 — 라벨을 `failed` 일 때 "다시 입기"(`atelier.wearRetry`, 3국어)로 바꿔 재시도 어포던스 명시(새 버튼 없이 기존 버튼 재사용).
- [x] **UX-MOBILE-1** (High) 모바일 도크 `max-h-75vh` 협소(`RightPanel`:31)·터치타겟<44px(`HostConsole`:339·`EmoteConsoleCard`:44) → 높이 계층화 + `py-2`. **구현됨(2026-07-14):** `RightPanel` max-h 계층화(75→sm:80→md:85vh→lg:calc, 큰 폰/태블릿 채팅 여유↑)·`HostConsole` 무대초대 버튼 + `EmoteConsoleCard` 이모트 버튼에 `min-h-[44px]`(44px 터치타겟 보장). 세로 전용 변경이라 360px 가로 오버플로 무영향. tsc·lint·test147.
- [x] **UX-A11Y-1** (Med) 색상단독 상태(믹서 `RoomBottomBar`:67·네트워크·speaking `StageSlot`:25)+서버에러 원문 노출(`CommissionCorner`:186)+모바일 공유숨김(`RoomTopBar`:40) → aria-label/아이콘 병행·에러 i18n 재매핑. **구현됨(2026-07-14):** ①서버에러 원문 노출 해소 — `CommissionCorner` catch 가 `e.message` 원문 대신 로컬라이즈 `orderFailedToast` 표시, 원문은 `console.error` 보존. ②모바일 공유숨김 해소 — `RoomTopBar` 공유 버튼 모바일 아이콘 전용(🔗+`aria-label`)·sm↑ 텍스트, 컨테이너 `flex-wrap`(모바일)으로 360px 가로 오버플로 차단(개별 아이템<328·제목 truncate·CSS 불변식)·desktop `sm:flex-nowrap` 원복. **재판정(이미 접근가능이라 무변경):** 네트워크=텍스트 라벨+`role=status aria-live` 병행·믹서=`aria-pressed`·speaking=`scale-105`(크기=비색상 단서, WCAG 1.4.1 충족). check:responsive 기본 라우트는 인증 룸 미커버(로그인 리다이렉트)라 flex-wrap 불변식으로 판정.

**A-P1e (룸 페이지 누락기능 감사 2026-07-17 · dogfood 3차 — 정찰3+Opus 페르소나3+메인 원본대조)** — 신규 확정 갭 6. ~~손들기(ROOM-20 뷰어 버튼 복원)~~ **폐기(주인님 결정 2026-07-17)** — 단, 무대 승격(ROOM-21)은 서버가 "손든 뷰어"만 초대 허용이라 시작점 없이 잔존 → 승격을 살리려면 invite-to-stage 를 손들기 무관으로 바꾸는 후속 결정 필요.
- [x] **RM-HOSTXFER** (High) 명시적 호스트 이양 부재 — 자동 승계만(HOST-06 후반·G-187), `lib/rooms.ts`·Edge 에 transfer 경로 0. **구현됨(2026-07-17, GOAL-room-gaps R1):** Edge `transfer-host`(requireHostRoom→대상 활성배우 검증→조건부 host_id 갱신→'host_change' broadcast best-effort) + `transferHost` 래퍼 + RoomPage 수신(전원 멤버 재조회로 isHost·왕관·콘솔탭·vod publisher 재파생, 새 호스트 toast) + HostConsole 이양 버튼(배우만)+확인 모달 + i18n 9키×3. 'host_change' 는 서버릴레이만 수락(SEC-RA-1 화이트리스트 미등재=클라 발행 자동 드롭). 실측: deno check clean·check:all(159/159·build·docs). ✅ **프로드 배포+라이브 실증(2026-07-17 bepo, transfer-host v1)**: 이양→rooms.host_id 실측·구호스트 403·셀프 400·신호스트 권한 복귀 200(2탭 UI 클릭스루는 기존 raise_hand 재조회 경로 재사용이라 생략). <!-- probe: supabase/functions/transfer-host/index.ts :: requireHostRoom -->
- [x] **RM-EDIT** (High) 방 설정 편집 부재 — 생성 후 제목/장르 변경 경로 0(`lib/rooms.ts`·functions 실측 0건). **구현됨(2026-07-17, GOAL-room-gaps R2):** Edge `update-room-settings`(requireHostRoom·title 1~80 create-room 미러·GENRES 화이트리스트('' =제거)·'room_update' broadcast 에 병합 최종값) + `updateRoomSettings` 래퍼 + RoomPage 수신(전원 상단바 즉시 반영)+저장 콜백 + HostConsole "방 설정" 섹션(제목 input+장르 select, lobby.genre* 재사용) + i18n 5키×3. 실측: deno check clean·check:all(159/159). ✅ **프로드 배포+라이브 실증(2026-07-17 bepo, update-room-settings v1)**: 비호스트 403·호스트 편집 200·장르 화이트리스트 400·rooms 행 반영 실측. <!-- probe: supabase/functions/update-room-settings/index.ts :: requireHostRoom -->
- [x] **RM-GUEST-CTA** (Med) 게스트 전환 퍼널 데드엔드 — 익명 관전자 채팅 잠금이 문구뿐(`ChatPanel.tsx:153-156`), 방 안에서 로그인/가입 경로 0. **구현됨(2026-07-17, GOAL-room-gaps R3):** ChatPanel 잠금 블록에 로그인 CTA — 네비는 상위 주입 `onGuestCta`(onSend 동형, 라우터 비의존 유지·유닛테스트 무회귀), RoomPage 가 `state.from=현 URL(?watch=1 보존)` 로 LoginPage 원목적지 복귀 재사용. i18n 1키×3. 실측: check:all(159/159 — 최초 라우터훅 안이 chatPanelMeta 테스트 FAIL → 콜백 prop 정수정 후 그린). <!-- probe: src/features/chat/ChatPanel.tsx :: onGuestCta -->
- [x] **RM-MUTE-DUR** (Med) duration 임시 음소거 미구현 — `setParticipantMute` 토글만(HOST-08 스펙: target+duration). **구현됨(2026-07-17, GOAL-room-gaps R4):** `muted_until` 컬럼은 마이그 `20260702050002` 선제 생성분 재사용(신규 마이그 0·스키마 문서 기존재). set-participant-mute duration_sec(10~86400)+**만료 자가해제 경로**(본인 muted=false 호출 시 서버 시계 검증) + 만료 파생 3점(livekit-token canPublish·list-room-members 응답 단일지점 파생·RoomPage 본인 만료 타이머) + HostConsole 길이 셀렉트(무기한/1/5/10/30분)+만료시각 표시 + unmute 시 viewer canPublish 오부여 잠복버그 role 파생 정수정 + i18n 4키×3. 실측: deno check ×3 clean·check:all(159/159). ✅ **프로드 배포+라이브 실증(2026-07-17 bepo, v8·list v10·token v14)**: duration mute 200+muted_until·<10s 400·만료 전 자가해제 403·**실시간 10s 만료 후 자가해제 200**·명단 파생 해제 반영 실측. <!-- probe: supabase/functions/set-participant-mute/index.ts :: duration_sec -->
- [x] **RM-SOFTLEAVE** (Med) 탭닫기 soft-leave 미실행 — `useLiveKitRoom.ts:407-413` 언마운트 정리만, leave-room 미호출 → 호스트 공석·유령 참가자(로비 인원 오염). **구현됨(2026-07-17, GOAL-room-gaps R5):** ①근본 `livekit-webhook` 신규(WebhookReceiver 서명검증 401·participant_left→**재실 대조**(listParticipants — 리로드/순단 오탐 차단)→soft-leave+승계, verify_jwt=false는 vgen-webhook 동형 config) ②승계 로직 `_shared/roomLeave.ts` 추출(leave-room 과 공유·응답 계약 불변) ③완화 pagehide keepalive fetch(`leaveRoomKeepalive`) — **비호스트 한정**(호스트 리로드 승계 오발화 회귀 방지·호스트 공석은 웹훅이 회수)·bfcache persisted 스킵. 실측: deno check ×2 clean·check:all(159/159). ✅ **프로드 배포+라이브 실증(2026-07-17 bepo, livekit-webhook v1·leave-room v13)**: verify_jwt 해제 라이브·실서명 HS256 participant_left → DB soft-left·위조 401·leave 회귀(ended). ✅ **실 탭킬 E2E 확정(`lk` CLI, 2회 재현)**: 대시보드 webhook 등록 후 `lk room join` 실참가 → 프로세스 SIGKILL → **LiveKit 이 ~20s 후 참가자 제거(participant_left 발화) + 그 순간 DB soft-left 회수**(26s·27s 2회). **앞선 브라우저 SIGKILL 2회 실패는 제품결함 아닌 테스트 하네스 artifact**(playwright browser.process() 부재 → pkill 이 Chrome LiveKit 연결을 못 끊어 disconnect 미발생). LiveKit CLI 실참가로 깨끗이 재현. <!-- probe: supabase/functions/livekit-webhook/index.ts :: WebhookReceiver -->
- [x] **RM-REAP** (Med·재감사 2026-07-17 — 주인님 "아무도 안 쓰는데 방이 계속 남음" 관측) 좀비 방 미회수 — 방 종료가 `softLeaveRoom`(마지막 배우 나감)에만 의존(`_shared/roomLeave.ts:50`). leave-room·livekit-webhook(RM-SOFTLEAVE) 둘 다 못 뜨는 경로 — **호스트가 방만 만들고 LiveKit 실접속 전 이탈**(`create-room:50,58`이 waiting+connected 생성)·webhook 등록 이전 방·전달 실패 — 는 참가자 행이 `connected`로 영구 고착 → 로비 좀비. DB만으론 진짜 빈 방을 못 가림(거마비 사례: 좀비와 동일한 waiting·cur=1인데 LiveKit엔 실접속). **실측(2026-07-17):** 프로드 비-ended 21방 중 15방이 4.7~14일 stale(전부 테스트방 test·r14/r17/r11-e2e·dasf) → 수동 `status='ended'` 정리(로비 17→2). reaper 크론 0(마이그/함수 grep 확인). **정수정:** LiveKit presence 대조 reaper — 새 Edge `reap-stale-rooms`(REAPER_SECRET 가드·waiting/live∧¬studio/practice/demo∧생성10분경과 후보마다 `listParticipants`→0명이면 종료+행 soft-left, presence>0=유지) + pg_cron(*/5)→`net.http_post`(vault 'reaper_secret' bearer — 이 레포 첫 크론→Edge, 서비스키 대신 전용 시크릿: 런타임 `SUPABASE_SERVICE_ROLE_KEY`가 신형 sb_secret 41자라 매칭 취약). **배포+라이브 실증(2026-07-17):** 함수 v3·pg_net 0.20.3 설치·cron 등재·vault 심음. **positive-control(`lk room join` 실참가)**: presence 1 → `reaped:0` 방 보호 / SIGKILL ~20s 후 → `reaped:1` 회수 / 틀린·빈 bearer 401 / 크론 경로 `net._http_response` 200. <!-- probe: supabase/functions/reap-stale-rooms/index.ts :: REAPER_SECRET -->
- [x] **RM-REJOIN** (High·주인님 실사용 관측 2026-07-19 "Token fetch failed: 403 — Not a participant 가끔 발생, 새로고침하면 입장됨") 백그라운드/절전 복귀 토큰 403 데드엔드 — 원인 사슬(코드 실증): 연결 사망 → `livekit-webhook` participant_left 가 내 행 soft-leave → 복귀 시 세션 리프레시로 connect effect 재발화(`useLiveKitRoom.ts` 토큰 재발급)가 `livekit-token:73` 403 에 재시도 없이 FAILED 종착. 재입장 직후 늦은 웹훅이 재실 대조 시점(내 LiveKit 재접속 전)에 갓 재활성된 행을 도로 회수하는 레이스가 "가끔"의 정체. 새로고침은 조인 머신이 joinRoom(멱등) 후에만 토큰을 떠서 항상 성공. **정수정(2026-07-19):** 토큰 403 "Not a participant" 시 멱등 재조인→재시도 자가치유(최대 2회 — 이탈 이벤트는 접속당 1발이라 수렴, cancelled 가드·viewer 경로 분기·재조인 후 setRoomContext 서버진실 갱신). 웹훅/마이그 변경 0. **실측:** 하네스 `rejoin-heal-e2e.mjs` PASS 2/2 — A(위조 403 주입→`[RM-REJOIN]` 치유 로그→재연결·트랙 재발행·에러 UI 0) · B(실 DB soft-leave 모사+세션 리프레시→활성 행 복원·무대 유지) + test 159/159. ✅ **bepo(2026-07-19)**: CF Pages `index-CQsjwipf.js` 별칭 byte-identical(`cmp`)·curl 3종 200·번들 시크릿 6종 clean·heal 조건 문자열 서빙 1건·DEV 훅(`__supabase`·치유 로그) 프로드 0건(tree-shake). 인접 관측: 솔로 방은 같은 레이스에서 "마지막 배우 이탈"로 방 자체가 ended→재조인 409(하네스 1차 실측 — 활성 더빙 방은 DUB-PERSIST 가 보호. 유예 없는 즉시 종료가 맞는지는 별도 판단). <!-- probe: src/hooks/useLiveKitRoom.ts :: RM-REJOIN -->
- [ ] **RM-SCRIPT** (High·**대형**·보류 2026-07-17) 방별 대본 선택 부재 — 전 방 `SEED_SCRIPTS[0]` 고정(`useScriptSync.ts:61`), 대본 시스템 전체가 데모 1개에 묶임. ~~마이그 rooms.script_id+시드 N종~~ → **스펙 정본 재설계 대상(주인님 결정)**: G-286 방향(scripts 테이블·DB cues_json) + CNT-02 사용자 업로드(P1) + CNT-09 시드 팩(P1) — 별도 세션 플랜모드 설계 승인 후 착수. G-63 즉흥 게이트와 동근원. **시드 소스 결정(2026-07-19 주인님)**: 공용도메인 희곡 경로 폐기 — 시드는 지정 유튜브 클립(Aniplus 무직전생 2화 하이라이트) 다운로드 사용. 저작권 조사 문서(research/SCRIPT-SEED-COPYRIGHT.md)는 제거(요지는 GAP-MATRIX 2026-07-19 로그 보존). ⚠️방송사 공식 클립 = 저작권물: 사적 도그푸딩 소스 무해(현행 업로드 패턴 동일), 플랫폼 내장 시드로 공개 배포 시 재검토. 잔여 조사(설계 세션): Rehearsal Pro/ColdRead 리허설 UX 델타·Fountain 화자: 파싱 규칙·송북형 선택 UX(**역할 수 ≤6 필터**가 킬러 축). **데모 시드 제거됨(2026-07-19 주인님 "실제 데이터만")**: "첫눈 오는 날"은 DB 아닌 `cues.ts` 하드코딩(scripts 테이블 404 실측) → SEED_SCRIPTS 비움·`useScriptSync` null 허용·좌패널 빈 상태(`script.noScript` 3국어) — 게이트 165/165·경고 0. 대본 기능은 실데이터 공급(이 행 정본 설계) 전까지 휴면.

**UX 델타 2026-07-17 (트랙 B · 룸 감사 1회 패스)** — **6건 전부 구현됨(2026-07-17, GOAL-room-gaps R7)**: 채팅 탭 미읽음 뱃지(RightPanel badge+구독 콜백 카운트·9+ 캡)·무대초대 수락 실패 toast+승격 재연결 안내·더빙 내 세그먼트 앰버 강조(호스트=선택값/비호스트=트랙 진실)·좌석 번호 칩(aria-hidden)·노트 휘발성 고지·DUB 탭 뷰어 안내 1줄 — i18n 7키×3. 실측: check:all(159/159 — 컴파일러 lint 2FAIL(렌더 ref·effect setState)→스토어 구독 패턴 정수정). HOST-12 통합 오버레이는 대형 → LATER. <!-- probe: src/features/room/RightPanel.tsx :: badge --> **반증(오탐 기각)**: 하이쿠 ORPHAN 16건 전부 배선됨·ROOM-18 배속 칩 구현됨(`MainView:91-106`)·모바일 하단바 상시 고정(RoomShell 3행 grid)·본인 스피킹 표시 존재(StageSlot scale+glow)·비호스트 스크럽 복귀는 계약 의도. **문서 드리프트**: FEATURE-SPEC/GAP-MATRIX 룸 상태가 코드보다 뒤짐(ROOM-17/20/22·HOST-09/10/11 "미착수" 오탐 6건) — /마감 시 상태판 갱신.

**더빙 라이브 검증 델타 2026-07-17 ("더빙 고도화" 착수 전 완성형 프로드 실주행)** — 더빙 파이프라인 완성·고속 실증(무직전생 59s 클립 프로드: STT 6.5s·번역 6.8s·음원분리 21.7s, JP→KR 대사 정확). 라이브가 간판 경로 P0 잠복버그 발견 → 정수정 배포.
- [x] **DUB-LANG** (P0·간판경로 무오류 실패) 더빙 소스 언어가 방 UI 언어(`rooms.language`)에 하드바인딩 — STT 힌트(`start-dub-transcription:73`)·번역 소스언어(`translate-dub-script:71`) 둘 다 `room.language`이고 `createRoom()`(`lib/rooms.ts:24`)은 language 미전송·생성/편집 UI 선택기 0 → **UI로 만든 방 100% 소스언어='ko' 고정** → 일본/영어 영상 STT 오인식+번역 스킵(무에러, 결과만 깨짐). **정수정:** `dub_sessions.source_language`(방 UI 언어와 분리·마이그 `20260717130000`·CHECK ko/en/ja·스키마 split byte-gate 3파일 동기) + 세 함수 `source_language ?? room.language ?? 'ko'` 폴백(legacy null 회귀0) + DubPanel 원본언어 select(기본 ja) + i18n 4키×3. 실측: check:all(tsc·lint·test·build)·docs:schema byte-gate·deno check ×3. ✅ **프로드 배포+라이브 A/B 실증(2026-07-17 bepo)**: 방 `language='ko'` 고정 대조 — **A**(source='ja')→STT 일본어 정확+JP→KR 번역 11 / **B**(미지정 null)→'ko' 폴백=일본어 오인식+번역 스킵(회귀0 확인). functions 범프(create-dub-session v10·start-dub-transcription v12·translate-dub-script v9)·psql `source_language` 컬럼+CHECK 실존·CF Pages curl 3종 200. **후순위 재확정(라이브 근거):** 화자 자동분리·서버측 합성·스템 캐시(재합성 21.7s+$0.04 재과금). <!-- probe: supabase/functions/start-dub-transcription/index.ts :: source_language -->
- [x] **DUB-PERSIST** (P0·유료작업 보존 — 주인님 "방 나가면 유료 업로드가 날아가나" 관측) 업로드는 STT+번역 비용이 드는데 방이 종료되면(soft-leave 마지막 배우 OR RM-REAP reaper) `join-public-room`(:34) 409 로 재입장 막혀 더빙 고립(데이터는 DB/R2 영속·지우는 코드 없음·재입장 시 `fetchActiveDubSession` 복원하나, 방이 ended 면 도달 불가). **정수정:** 활성 더빙 세션(status NOT IN completed/failed) 있는 방은 `_shared/roomLeave.ts`·`reap-stale-rooms` 둘 다 종료 스킵(is_practice 유지 예외 동형) → 방 살아있으면 기존 복원 그대로(추가 UI 0). **배포+라이브 실증(2026-07-17 커밋 5a54db6)**: 활성더빙 방 leave→`waiting` 유지 · reaper checked2/reaped1(더빙방 보호·대조군 회수) · fetchActiveDubSession 복원. <!-- probe: supabase/functions/reap-stale-rooms/index.ts :: activeDub -->
- [x] **DUB-UX** (구조·패널 연결 — 주인님 "메인뷰 영상 안뜸·왼쪽 안바뀜·패널 연결 끊김·역할저장 어렵고 다음 모름") 더빙이 오른쪽 패널에 고립(구조 감사: `stageStore.mode='dub'`를 MainView·ScriptPanel 미구독·소스는 DubRecorder `<audio>` 참조뿐·좌패널 연기대본 하드락). **정수정 4페이즈:** ①업로드 어포던스(📁영상선택 버튼+파일명+비활성이유 힌트)+자동 파이프라인(업로드→STT→번역 자동연쇄) ②`dubStore` 신설→MainView 센터 소스영상+현재 세그먼트 자막(vodSync 재사용·원음 음소거) ③`DubScriptPanel` 좌도크 더빙대본 동기 하이라이트 ④단계표시기(①업로드②역할③동의④녹음⑤완성)+솔로 자동배정 요약+각단계 안내+포커스/호버. `check:all` 159/159 각 phase. **배포(CF Pages bd2d337d·curl3종200·번들시크릿 clean)** — 라이브 육안 검증 대기. 후순위(범위밖): 화자 자동분리·서버합성·RM-SCRIPT 연기대본 시스템. <!-- probe: src/stores/dubStore.ts :: currentSegmentId -->
- [x] **DUB-FRICTION** (/감사 3페르소나 도그푸딩 — 신규유저·호스트·뷰어/모바일/a11y, 배포 상태 재감사) 잔여 UX 마찰 발굴·검증·해소. **해소(검증됨):** ①실패 데드엔드(status='failed' 메시지만·재시도 0)→`start-dub-transcription` 이 'failed'도 수용 + DubPanel 재시도 버튼(STT+번역 재연쇄) ②대본 기본 원문(일본어) 표시→번역본 기본(showTranslation 기본 true·신규가 한국어 자막 못 보던 것) ③센터 자막 대비(bg-black/70→85+text-shadow, WCAG) ④DubRecorder 진행 `aria-live`. **반증(REFUTED):** 호스트 이양 후 더빙 확인/녹음/확정 권한=confirm-dub-track·start-dub-recording·submit-dub-output 전부 `rooms.host_id` 체크(이양 시 갱신)→안전(워커 `created_by` 오판) · 뷰어 좌패널 더빙대본=편집 host-gated read-only라 무해. **잔여(후속):** 다인 참가자 진행 타임라인·합성 ETA·소스언어 사후변경·리액션휠 편집 발견성. check:all 159/159·배포(34297fb6). <!-- probe: supabase/functions/start-dub-transcription/index.ts :: failed -->
- [x] **DUB-TANGIBLE** (G9 — 주인님 "녹음이 되는지 확인할 길이 없고 결과가 영상에 바로 안 보임") 녹음 체감 4종: ①마이크 실시간 레벨미터+무음 2.5s 경고(AnalyserNode·destination 미연결=에코 방지) ②녹음 로컬모드(시작→센터 영상이 그 구간을 음소거 재생·vodSync 발행/수신/하트비트 게이트·구간끝 정지 유도)+중지 즉시 원본영상 동기 미리보기(`lib/dubPreview` Web Audio 스케줄·ffmpeg 불필요) ③누적 시사회 "지금까지 더빙 ▶"(호스트 토글→전원 각자 페치·스케줄 — get-dub-recordings 멤버+submitted 완화 v13 배포·SEC-RA-1 dub_screening 호스트 identity 게이트) ④내차례 배너(dubStore.myTurnRanges)+±200ms 캘리브레이션(미리보기·합성 동일 적용 — submit-dub-track v10 배포·DubCompositor cue+cal). 실측: 각 phase check:all 159/159 + 헤드리스 가짜마이크 실렌더 4하네스 PASS(P2 12어서션·P3 2탭 broadcast·P4 DB cal=200). ✅ **bepo**(2026-07-18: CF `index-DpVmQqc7.js` 별칭 byte-identical·curl 3종 200·시크릿 clean·**배포판 스모크 8/8** — 레벨미터 실측·REC 배지·시크·DB cal=200). 설계·실행기록 `goals/GOAL-dub-recording-tangible.md`. <!-- probe: src/lib/dubPreview.ts :: playDubPreview -->
- [x] **DUB-RETAKE** (주인님 실사용 관측 2026-07-18 "한번 녹음한 것을 되돌릴 수 있나") synced 후 되돌리기 부재 + `submit-dub-track` 트랙 status 무게이트(API 로 확정 우회 가능). **구현됨(2026-07-19, 플랜 snoopy-rolling-sutton):** ①`submit-dub-track` 서버 게이트 — synced 재제출 409(assigned/recording/submitted 만) ②`confirm-dub-track` `undo` 확장 — 호스트 전용 synced→submitted 확정 해제(세션 recording 일 때만·합성 중/완료 409, all_synced 재계산 응답) ③호스트 [확정 해제] 버튼(`DubRecorder`) — 해제 시 Realtime 전파로 배우 [● 녹음] 자동 복귀(기존 조건 재사용, 추가 상태 0) + i18n 2키×3 ④레벨미터 하드닝 — `ctx.resume()` + suspended 구간 무음판정 미산입(문턱 0.02 유지). 배우측 "재녹음 요청" 알림 흐름은 YAGNI(라이브 음성 방 — 구두 요청). **실측:** deno check 2함수 clean · check:all(tsc·lint·test 159/159·build·docs). ✅ **bepo(2026-07-19)**: submit-dub-track v11·confirm-dub-track v10(functions list 갱신 오늘 실측) + **프로드 라이브 9/9**(`retake-prod-verify.mjs`, service-role 시딩 무과금) — 제출→확정→**synced 재제출 409**→비호스트 undo 403→**호스트 undo 200/submitted·all_synced false**→submitted 에 undo 409→**재제출 200(재테이크 개통)**→재확정 synced→**compositing 중 undo 409**. <!-- probe: supabase/functions/confirm-dub-track/index.ts :: undo -->
- [x] **DUB-TRIM** (주인님 제안 2026-07-19 "캡컷처럼 원하는 구간을 트림해서 그 구간만 더빙 + 수정/삭제를 전원 리얼타임 + 피그마식 편집중 표시") 난이도 평가 완료: 트림=세그먼트 시간창 제한(재인코딩 불필요·Realtime 기존 구독이 전파)=낮음~중간 / 세그먼트 협업 편집=충돌 처리가 본체(CRDT 오버킬 — 호스트 단독 v1→세그먼트 소프트락+편집중 배지 v2, 기존 호스트 권위 패턴 동형)=중간 / 닉네임 커서=전송은 사소(블렌드셰이프 채널 재사용)·리스트 UI 라 세그먼트 presence 배지가 실가치. **귀속:** ISS-07 "멀티트랙 영상 편집기" 대설계의 1슬라이스로 설계(트림 단독 선행 가능) — 별도 세션 플랜모드. **설계 전 조사 완료(2026-07-19, [research/DUB-TRIM-UX-REFERENCES.md](research/DUB-TRIM-UX-REFERENCES.md) + `design/product-ui-references/` 6종 UX-NOTES)**: 4축 권고 확보 — 트림=자막(세그먼트) 선택+드래그 핸들+스냅 토글(CapCut×유튜브 클립) · 세그먼트 편집=원본/배우대사 이중 컬럼+길이 배지(Rask×Descript) · presence=아바타 배지+소프트락 타임아웃+LWW(CRDT 오버킬 확정, Notion 패턴) · 역할배정=색상 파트 버튼 1클릭+진행도 뱃지(Smule). **v1 트림-단독 구현됨(2026-07-19, 플랜 승인·phase-loop P1~P2)**: 확정 3결정(A안 업로드 전 트림·유튜브 수동 다운로드→파일 업로드·키프레임 스냅 -c copy) 그대로 — `trimVideo()`+순수부(clampTrimRange·estimateTrimBytes, lib/ffmpeg.ts 합성 모듈 재사용·신규 의존성 0) + DubPanel 트림 스텝(프리뷰 video·range 슬라이더 2·선택구간 재생·예상 용량/25MB 경고·기본=전체라 무회귀·절단 후 실측 25MB 재판정) + i18n 8키×3. 근거: 부분 배정은 서버 허용이나 합성이 전체 보컬 제거라 미배정 구간 무음 입뻥긋 — 장면 더빙의 정답은 트림. **실측**: check:all 165/165(+trimMath 6) · 헤드리스 E2E `dub-trim-e2e.mjs` **PASS 5/5**(yt-src 59.4s → 5~20s 절단: 스토리지 801KB=원본 29%·STT 3세그 maxEnd 13s=절단본 기준+비용 구간 비례 / 전체 범위 = 원본 byte-identical 무회귀). ⏳ **배포 대기(CF Pages만 — 서버 변경 0)**. 잔여(대설계 본편 별도): 세그먼트 시간수정·분할·협업 편집·presence 배지·앱 내 유튜브 인입. 토끼굴 금지: CRDT 비교·프로 NLE·서버합성. <!-- probe: src/lib/ffmpeg.ts :: trimVideo -->
- [x] **DUB-ONBOARD** (→ **DUB-SHOWCASE 사다리 S4 로 흡수**, 2026-07-19 플랜 승인 — [goals/GOAL-dub-showcase.md](goals/GOAL-dub-showcase.md)) (주인님 질의 2026-07-19 "업로드→역할→동의→녹음→완성 5단계, 진입장벽 너무 높나") 평가: **다인 협업 기준으론 각 단계 정당**(역할=인간 결정 필수·동의=녹음 법적 요건 G-43 이라 제거 불가, 이동/병합만 가능) — **솔로/소인원에서 과함**이 진단. 간소화안: **A(추천·소형) 솔로 패스트패스** — 멤버 1명이면 STT 후 [바로 녹음 시작] 버튼 1개(역할 전원 자동 배정 + 동의 인라인 체크 1개 → assign→consent→start 클라 자동 연쇄, 서버 게이트 불변) = 체감 5→2단계 / B(중형) 다인 JIT 동의 — 사전 동의 스텝을 각자 첫 [● 녹음] 클릭 인라인으로(단 start-dub-recording all_consented 게이트 재설계 = SecurityPolicies §11 검토 필요, 정본 설계감) / C(소형) 단계표시기 유지 + "다음 할 일" 큰 버튼 승격(부분 존재). 착수는 A 부터. **완료(2026-07-19 DUB-SHOWCASE S4)**: 솔로 ready = [🎙 이 장면, 내 목소리로] 원버튼(+동의 인라인 체크) — assign→consent→start 클라 자동 연쇄(서버 게이트 3종 불변)·단계표시기 솔로 숨김·용어 원어→기존("기존 목소리를 걷어내는 중…"). S5 실측: 1클릭 → 전세그 배정 3·all_consented·recording + 표시기 숨김. 더빙 무대 리레이아웃(S3 — 씬 배경 제거·AR fit·bare 아바타 오버레이)도 동편 완료. <!-- probe: src/features/dub/DubRoleAssigner.tsx :: soloStartButton -->
- [x] **DUB-BED** (→ **DUB-SHOWCASE 사다리 S1~S3 로 흡수**, 2026-07-19 플랜 승인 — [goals/GOAL-dub-showcase.md](goals/GOAL-dub-showcase.md) · +디렉티브: 용어 원어→기존·더빙 모드 배경 제거·AR fit 최대화·bare 아바타 오버레이) (주인님 관측 2026-07-19 "배경음에서 음성만 추출할 수가 없어서 이 모든 과정이 애매 — 더빙의 핵심 기술 아닌가") **사실관계**: 그 기술(보컬 제거)은 이미 있음 — `separate-dub-audio`(fal Demucs)가 원어 대사를 제거한 배경 스템을 만들어 합성이 그 위에 더빙을 얹음(2026-07-17 라이브 실증 21.7s·이중음성 없음). **진짜 문제 = 분리가 맨 끝(합성)에서만 돌아 체감 부재**: 녹음 중 센터는 음소거·미리보기/시사회는 배경음 0(내 목소리만 무음 배경에) → "음성 뺀 배경 위에 내 연기" 체감이 완성본까지 안 옴. **정수정**: ①분리를 STT 후 자동 연쇄로 선행 + **스템 캐시**(dub_sessions에 background_urls 저장 — 합성 재과금 $0.04 절약, 기존 defer 부활) ②미리보기·시사회 오디오에 배경 스템 합류(`dubPreview`에 베드 트랙 1개 추가 — Web Audio 스케줄 그대로) ③(옵션) 녹음 중 배경 모니터 토글(헤드폰 전제·기본 off). 한계 정직: Demucs 2-stem 은 대사 잔류 가능(애니 BGM 은 양호) — 업그레이드 경로 AudioShake([[dub-audio-separation-anime]]). 비용: 분리 1회는 어차피 합성 때 낼 비용의 선지불이라 순증 0(캐시 시 오히려 절감). **각인 UX 확장(2026-07-19 상의 — "Demucs 로 본질을 한번에 각인")**: remove.bg 순간의 오디오판 = 센터 **[원본|배경만] A/B 토글**(탭 1로 원어 목소리 소멸 청취, 각인 #1) + 중지 즉시 배경 베드 위 재생(각인 #2) + 솔로 원버튼 [이 장면, 내 목소리로](DUB-ONBOARD A안 통합·서버 무변경) + 진행 카피 내레이션("원어 목소리를 걷어내는 중…"). 스템은 fal CDN 반환·미보존 실측 → **우리 버킷 결정론 경로 복사 = 마이그 0 캐시**. 사다리: B1 분리선행+캐시 → B2 센터 베드/토글 → B3 솔로 원버튼 → B4 카피. **완료(2026-07-19 DUB-SHOWCASE S1~S3)**: separate-dub-audio **v12 배포** — 매니페스트 버킷 캐시(1회차 실분리 96s·스템 5개 2.4MB 복사 → **재호출 639ms cached:true·재과금 0**·멤버 캐시-전용 개방·레이트리밋 미스만 계수) + 업로드 체인 꼬리 분리 선행 + 센터 [기존 소리|배경만] A/B 토글(S5 실측: 배경만=video 음소거+베드 5/5 재생 ↔ 기존 소리=소리 복원+베드 0) + 미리보기·시사회 베드 합류(dubPreview bed 트랙). <!-- probe: supabase/functions/separate-dub-audio/index.ts :: stemBase -->
- [x] **DUB-VISIBILITY** (주인님 실사용 관측 2026-07-19 "포커스 다른 곳에 있다가 복귀하니 업로드가 사라짐 — 복구되나") **데이터 유실 0 확정(DB 실측)**: 방 'ㅇㄴㅁㄹ' waiting·세션 3개 생존(최신 recording). **원인 사슬(코드+DB 실증)**: 백그라운드 중 웹훅 soft-leave 로 참가 행 left → 복귀 직후 `fetchActiveDubSession`(클라 anon+RLS)이 재조인보다 먼저 돌면 `is_dub_member`(state<>'left')가 세션을 **숨김** → 빈 업로드 화면("사라진 것처럼") → dub_* 는 안 바뀌어 Realtime 재조회 트리거도 없음 → 유저가 재업로드(같은 방 세션 3개·left 행 60개가 물증 — 유료 2회 중복). **정수정(2026-07-19)**: DubPanel 이 LiveKit `CONNECTED`(=조인 완료=행 활성) 시 refresh 재실행 — 레이스 원천 봉함. 실측: check:all 165/165. ⏳ 배포 대기(CF 합류). defer: left 행 축적 정리(방당 수십 행 — 무해하나 위생, 재조인이 매번 새 행 insert), 세션 이력 선택 UI(현재 최신만 노출 — DUB-NEW 와 동군).
**주인님 실사용 피드백 배치 2026-07-19 (더빙 무대 도그푸딩 — 다음 세션 1순위·/감사 와 함께)**
- [ ] **DUB-CONSENT-VIEWER** (High·Confirmed — /감사 3기 2026-07-19, 메인 서버 대조 확정) **관전자가 있으면 녹음 시작 불가**: `record-consent` 의 all_consented 재계산(:54-60)이 `room_participants neq left` **전원(role 무관)** 을 요구 → 뷰어가 동의 안 하면(동의 개념도 없어야 함) 영구 false → `start-dub-recording` 게이트 미충족. +뷰어에게 동의 버튼·배정 UI 노출(DubPanel 이 isViewer 를 DubRoleAssigner/DubRecorder 로 미전달). **정수정**: 재계산·표시 전부 배우만(`role <> 'viewer'`) + 하위 컴포넌트 isViewer 게이트.
- [ ] **DUB-CHAIN-DONE** (Med — 솔로 워커 #1+#4 통합) 자동 체인(STT→번역→분리) 완료 순간 "대본 준비 완료" 신호 부재(phase null 만) + 솔로 원버튼 클릭 후 진행 중 라벨 부재(busy 비활성만). 정수정: 체인 완료 toast.success + 원버튼 busy 라벨("녹음 준비 중…").
- [ ] **DUB-BED-LOCAL-HINT** (Med — 3기 공통) [기존 소리|배경만] 토글이 **각자 로컬**임이 암묵(협업 시 서로 다른 걸 듣는 상태 인지 불가) + 우측 패널(녹음 UI)에서 토글 존재·상태 비가시. 정수정: "내 화면만" 마이크로카피 + PANEL-UNIFY 정본 설계에 토글 노출 위치 포함.
- [ ] **DUB-CHANGE-NOTICE** (Med — 다인 워커, High 2건은 기각 후 잔여) 호스트의 세그 편집·확정 해제가 배우 화면에 **상태로는 실시간 수렴**(edit-dub-segment 트랙 미러+Realtime — E2 실측으로 데이터 desync 는 반증)하나 **명시 통지 없음**("네 세그가 옮겨짐/해제됨" 토스트·하이라이트). 정수정: 내 트랙 변경 감지 시 toast + 해당 블록 플래시.
- [ ] **DUB-A11Y-M** (Low 배치 — 뷰어/모바일/a11y 워커) 타임라인 드래그 핸들 키보드 대안 부재(pointer 전용·retime 불가) · 터치 타겟 미달(핸들 w-2=8px·줌/삭제 <44px) · 배정 색상 신호 보강(대비 검증) · 비호스트 ✏️ 부재 안내("호스트만 수정") · 시사회 의미 카피 1줄 · 모바일 컨트롤 스택(top-1/top-8) 겹침 검증.
- **/감사 반증 기각 기록(2026-07-19)**: 다인 워커 High "세그-트랙 매칭 깨짐·내차례 배너 불일치" = `edit-dub-segment` 트랙 미러(start/end 동시 갱신)+dub_tracks Realtime 재조회로 이미 동기(E2 5/5 실측) — 미러 미인지 오판 · "줌/베드 토글이 전원에 영향" = 둘 다 로컬 state(broadcast 없음) · 뷰어 줌 사용 가능 = 무해(관전 보조·의도).
- [ ] **DUB-HAIR-MATTE** (크로스레포·Vtube) bare 오버레이에서 아바타 머리카락 틈새에 흰 잔재 — 캔버스 투명화(α=0 실측)와 별개로 **리그 파트 텍스처의 매팅 잔재**(곱슬 틈새가 분해 때 안 파임 — 어깨머리 See-through 계보). 정수정 = Vtube 파이프라인 매팅 개선/해당 아바타 재빌드([[avatar-forge-crossrepo-sync]] — 착수 전 Vtube 병행세션 확인 관례). ChatterBox 측 완화 후보: 없음(CSS 불가).
- [ ] **DUB-AVATAR-DRAG** 오버레이 아바타 위치를 드래그드랍으로 조정 — 결정 필요: 로컬 취향 vs 방 공유(호스트 배치 broadcast). DubTimeline 핸들 드래그 패턴 재사용 가능.
- [ ] **DUB-SCRIPT-TELEPORT** 좌패널(DubScriptPanel) 세그 클릭 → 센터 영상 시크+타임라인 선택 텔레포트 — `dubStore.selectedSegmentId`+videoRef 시크(DubTimeline 클릭과 동형) 재사용, **소형**.
- [ ] **DUB-PANEL-UNIFY** (구조) 조작 동선 혼동 — "어느 패널로 조작해야 할지 헷갈림": 녹음 조작이 우측 패널에 고립, 주인님 제안 = 좌패널(대본) 중심 통합. 좌=대본+내 차례+녹음 버튼 / 우=관리(업로드·확정·합성)? 차기 /감사 발견과 합쳐 정본 설계(플랜모드).
- [ ] **DUB-STEP-BACK** 이전 단계 복귀 UX 부재 + 녹음(recording) 중 대본 수정 불가 — ①recording→ready 되돌림(서버 상태 역전이·H12 role-lock 해제 정책 설계 필요) ②텍스트 수정만은 recording 에서 허용 검토(`update-dub-segment-text` 게이트 ready→ready|recording — 텍스트는 기녹음 무손상이라 완화 후보, 시간/구조 잠금은 유지).
- [ ] **DUB-NEW** (주인님 질의 2026-07-19 "다른 영상을 새로 업로드해 더빙하려면 되나") 같은 방 "새 영상으로 새 더빙" **UI 진입점 부재** — 구조는 가능(`create-dub-session` 무가드 insert = 방당 세션 다수 허용·`fetchActiveDubSession` 최신 우선·기존 세션/R2 보존 90d), UI 만 세션 존재 시 업로드 브랜치(`DubPanel` `!status`)가 숨음(G-283 "새 더빙 리셋" defer 그 갭). 현 우회 = 새 방 생성. **정수정**: completed/failed 세션에서 호스트 전용 [새 영상으로 더빙] 버튼 → 업로드 브랜치 강제 표시 → 새 세션 생성 시 최신 우선으로 자연 전환(recording 중 교체는 협업 녹음 유실 위험이라 제외 또는 확인 모달). 소형 — /다음 1건 감.
- [x] **DUB-EDIT** (주인님 요구 2026-07-19 "메인뷰에서 캡컷·피그마처럼 드래그로 트림·수정·삭제 + 전원 실시간 + 편집중 닉네임" — 브리프 [goals/GOAL-dub-edit.md](goals/GOAL-dub-edit.md)) **센터 세그먼트 편집기 v1 구현됨(2026-07-19, 사다리 E0~E4 완주)**: ①`DubTimeline`(MainView 더빙 분기 flex 스택 — 블록=시간폭 비례 % 배치·플레이헤드 rAF·클릭=시크+`selectedSegmentId` 선택·배정 배우 색/이니셜) ②`edit-dub-segment` Edge **v1 배포**(retime/delete — **ready 잠금** 409·dub_tracks 미러) ③호스트 양끝 핸들 드래그(낙관 반영은 세그 배열 정체성 귀속 = 스냅백 자동해소)·[✕ 삭제]·Realtime 전파(기존 구독·transport 0)·`dub_edit` 편집중 배지(SEC-RA-1 화이트리스트·발신자명 수신측 확정) ④감사 픽스 5(제출 toast·역할저장 toast·submitted 대기 안내·합성 비활성 힌트·트림 펼침 이유) + i18n 9키×3. **실측**: check:all **165/165**(E1·E3·E4 각 회) · E2 프로드 시딩 통합 **5/5**(retime+미러·경계 400·비호스트 403·**recording 409**·delete cascade) · E1 실렌더 **5/5**(블록 6·이니셜·시크 20.0s·360px 오버플로 0) · **E3 2탭 E2E 5/5**(드래그 커밋 DB 13000→18716ms·멤버 블록 폭 35→75px 실시간·✎배지·삭제 동기·멤버 읽기전용). 감사 기각 1(확정 경고 모달 — RETAKE undo 로 가역). **v1.1 줌(2026-07-19 주인님 실사용 "너무 촘촘" — defer 서 부활)**: 캡컷식 −/맞춤/＋(×1~16, 스트립 폭=zoom×100% 로 % 배치 그대로 확장)·확대 시 플레이헤드 중앙 유지·재생 중 자동 스크롤 추적·확대 블록에 대사 미리보기·선택 블록 z-우선(빽빽할 때 핸들 보호). 실측: 하네스 6/6(스트립 1403px vs 뷰포트 416·블록 42→142px·대사 표시·fit 복귀 후 360px 오버플로 0)·check:all 165/165. ⏳ **배포 대기(CF Pages — edit-dub-segment 은 v1 라이브)**. defer: 분할·멤버 공동편집/소프트락·자유 커서·파형·핀치/휠 줌·모바일 터치 드래그·시사회 내 음성 표시·마이크 사전 테스트·Realtime 체감지연·트랙별 예상 길이. <!-- probe: src/features/dub/DubTimeline.tsx :: DubTimeline -->

**A-P2 (정합·권한 seam)**
- [x] **SEC-6** 일일한도 TOCTOU 원자화 — trigger-vgen count→insert 비원자 갭을 `check_rate_limit`(SEC-1/4 프리미티브 재사용) 원자증가로 대체. 캐시히트(무과금) 이후 배치 → 실제 생성만 계수·리셋없음(일일 누적 캡). **실측**(deno check clean; RPC는 SEC-1서 격리 postgres 실측·동일 프리미티브). ⏳ 배포 대기(SEC-1 마이그 `20260706130000` 의존).
- [x] **A-SEAM-4** 뷰어/모바일 **권한 로직**(seam) — 서버는 이미 강제 중(livekit-token `canPublish: role!=='viewer'`). 클라 seam: roomStore `myRole`(계약 ViewerGate.md §Store 의존성 `roomStore.role`, 3 조인/이탈 경로서 세팅) + 순수 `lib/roomPermissions.roomPermissions(role)`(서버규칙 미러) — B의 뷰어모드/MobileViewer가 읽음. **실측**(type-check·lint clean·**test 64/64** [+2 roomPermissions]). **범위 명시:** 전체 ViewerGate 라우트가드·뷰어조인·MobileViewer 레이아웃·invite 검증(`verify-invite-code`/`accept-invite` Edge **미구현**)·anon auth 는 P2 기능(seam 아님)으로 **잔여** — 현 join 은 actor 고정이라 뷰어 라이브 진입은 그 기능 뒤. 레이아웃은 B.
- [x] **A-SEAM-5** i18n **키 구조** — `i18n/coverage.ts`(`missingKeys`=B 번역 대기 worklist·`orphanKeys`=ko 없는 오타 키) + 가드 테스트(en/ja ⊆ ko 실증·오펀 0). en/ja 200키 복제 대신 커버리지 도구+구조 가드로 정비(fallback:ko 는 이미 동작). **실측**(type-check·lint clean·**test 67/67** [+3 i18nCoverage: en/ja 오펀 0·worklist 산출]). 번역 콘텐츠 채우기는 B.
- [x] **A-FUNC-3** 호스트 이양 배선 · mute 마운트 로드 — **이양(무배포):** hostId 를 멤버 이펙트서 `fetchRoomHostId`로 로드(참가자 변동=호스트 퇴장 시 재실행 → 새 호스트 반영), `isHost = host_id===내 users.id`(hostId 미로드 시 slot 프록시 폴백 → 2인 경로 무변화). **mute(배포 게이트):** list-room-members 가 `muted_by_host` 반환(옵셔널 `?? false` → stale 배포본 안전) + 멤버 이펙트서 내 mutedByHost 서버 재동기 + HostConsole 배지를 서버 진실서 파생(렌더 파생=prop동기 effect 없음, 새로고침 desync 제거). **실측**(type-check·lint clean·**test 67/67**·build PASS·deno check list-room-members clean). ⏳ mute 배지는 list-room-members 재배포 후 라이브(현 stale 배포본은 빈 배지=기존 동작).

### 트랙 B — UIUX 프레젠테이션 (나중 · 1회 패스 · 스펙: `design/UX-GAPS-AND-PATTERNS.md` + `uiux-distilled.md`)
- [x] **Toast/피드백 UI** (A-SEAM-1 채널에 스타일) — `ToastHost`(App 전역 마운트, 하단 중앙·의미색 고정·role=alert/status·4s 소멸·성공 체크 드로우·360px 안전) + emit 1 추가(리액션 릴레이 실패, cue 패턴 동형). **실측:** 게이트 4종 + 헤드리스 실렌더 5/5(3종 렌더·roles·4.2s 소멸·360px). 잔여 emit 사이트(채팅 전송표시·아바타 저장·방 생성 성공)는 §2 별도 행. *(2026-07-08)*
- [x] **Progress/ETA 바** (A-SEAM-2 데이터에 바) — 공유 `ProgressBar`(확정=씬액센트 채움+불씨 선단 / null=불확정 흐름, `--scene-accent` 연동 §4.3) + VGEN `VgenProgress`(1s 틱·95% 캡·남은초, `etaProgress` 순수함수+테스트 3) + DUB 합성 단계 바(믹싱만 실측 %). **실측:** 실렌더 4/4(바·aria-valuenow·채움폭·"남은 시간 약 85초"). 덤: 조인 취소 버튼(§2 룸진입 잔여)도 동시 완료 — joinRoom signal 관통+AbortError 조용 처리+모닥불 글리프, 실렌더 4/4(route 홀드→취소→로비·에러 미출현). *(2026-07-08)*
- [x] **반응형 레이아웃 + 터치 롱프레스 리액션 + 무대 동적 크기**(P-5) — 무대 `useSlotPx`(<480px→88px, 3열이 360px 안착) + 터치 롱프레스 ≥500ms→휠 sticky 개화(탭 선택) + 숫자키 1~N 즉발(입력필드 가드) + 룸 `p-4 sm:p-8`·로비 검색 `w-28 sm:w-40`. **실측:** 게이트 4종 + 헤드리스 9/9(데스크톱 120 유지·모바일 88·scrollWidth 360·롱프레스→sticky→탭 발사·핫키·입력중 미발사) + 360px 스크린샷. 잔여: 화면 전수 반응형 감사는 모바일 뷰어 작업과 함께. *(2026-07-08)*
- [x] **모바일 뷰어 레이아웃** (A-SEAM-4 위에) — 로비 수직기능 페이즈루프 Phase 4 로 기능 문과 함께 닫힘: `join-as-viewer`+`join_room_as_viewer` RPC(좌석·정원 비점유)·초대 role='viewer'·RoomPage 관전 모드(SelfAvatar 미마운트=웹캠 요청 0·"관전 중" 뱃지·마이크 버튼 없음)·로비 마감방 [관전] 폴백. **실측:** 프로드 15/15 — 2브라우저(배우 공연+뷰어 360px 관전: 호스트 아바타 원격 렌더·발행권 없음 토큰 실측·scrollWidth 360). 잔여: anon 뷰어(대시보드 anonymous sign-in 게이트)·MobileViewer 전용 뷰(UA 자동 다운그레이드). *(2026-07-08, GAP-MATRIX Phase 4 행)*
- [x] **다국어 번역 채우기** (A-SEAM-5 위에) — en/ja **232키 전량 완역**(기존 33키 유지·문체 통일, `{{보간}}`·이모지 보존). i18n.test 폴백 픽스처를 합성 프로브 키로 정수정(완역으로 실키 픽스처 소멸). **실측:** coverage missing 0·orphan 0(양언어) + test 70/70 + 실렌더 EN/JA 로그인 페이지 문자열 확인. *(2026-07-08)*
- [x] **모달 일원화**(P-4) — `components/shared/Modal.tsx`(role=dialog·aria-modal·포커스 트랩·Esc·복귀 포커스·백드롭) + 소비 2곳: 강퇴 확인(HostConsole 2단 토글 폐지 — 계약 §"최종 확인 모달" 충족) · CostConfirmDialog 전환(트랩 획득, props 불변). **실측:** 게이트 4종 + 2탭 E2E 7/7(dialog 개화·초점 진입·Tab 트랩·Esc 닫기·강퇴버튼 복귀 포커스·실강퇴 왕복·확정 후 닫힘). 비번 입장 "오버레이"는 오진 — 전체 페이지 렌더라 트랩 불요(autoFocus 존재). 잔여였던 강퇴 사유 필드는 **2026-07-10 해소**(kick-participant reason 수용 + 대상 한정 통지 + 모달 사유 입력, `ae32317`). *(2026-07-08)*
- [ ] **다크 elevation 그림자** · **라이트모드** (P2 폴리시 잔여)
- ~~대비 교정(UX-P6)~~ — 거짓양성으로 취소(WCAG 7.2:1).
- [x] **초대 에러 세분 배선** (델타 UX) — `verify-invite-code` 는 revoked/expired/used_up/room_ended 를 error 문자열로 구분 응답하는데 `LobbyPage` catch 가 단일 `inviteInvalid` 로 뭉갬(백엔드 seam 존재). `edgeFn` 이 error 를 `Error.message` 로 보존 → catch 에서 원인별 i18n 키 매핑(inviteExpired/Revoked/UsedUp/RoomEnded, en/ja 완역). **실측:** type-check 0·lint·test 75/75(i18nCoverage 포함)·build. *(2026-07-08)*
- ~~모바일 하단 네비 active state(워커 제안)~~ — 기각: 하단 네비는 광장(`/lobby`) 전용이고 interior 는 "← 광장으로" 복귀만(네비 non-persistent) → active 표시할 "현재 탭" 부재.

---

## §1. 보안 — 확정 (심각도순, 메인이 인용라인 직접 대조)

| ID | 취약점 | 심각도 | Confidence | file:line | 익스플로잇 | 정수정 |
|---|---|---|---|---|---|---|
| SEC-1 | 잠금방 비번 무제한 시도 | High | Confirmed | `join-room-with-password`(레이트리밋 전무·앱 전체 레이트리밋은 trigger-vgen 하나) | 4자리 비번=1만회 무차단 → 사설방 침입 | (user,room) 시도 제한 429 + 최소길이 상향 |
| SEC-2 | 스토리지 경로 traversal ×3 | High(R2 정규화 시)/Med | Likely | `submit-dub-track:39`·`submit-dub-output:48`·`create-dub-session:26` | 클라 경로 `startsWith`만 → `../`로 타방 파일키 저장 → 룸원 presign 접근 | 키 서버생성(클라 경로 미신뢰) + `..` 거부. R2 실테스트로 확정 |
| SEC-3 | SSRF (image_urls) | Med | Confirmed | `trigger-vgen:78-79`(`startsWith("http")`만) | 사설IP/메타데이터 URL 주입 → fal 페처 fetch (호스트전용·플래그 off 완화) | raw URL 폐기 → asset_id 서버서명 |
| SEC-4 | 무제한 비용 API | Med | Confirmed | refine-vgen·translate-dub·start-dub-transcription·separate-dub-audio(레이트리밋 0) | 호스트 루프 → OpenAI/Whisper/FAL 청구 부담 | 사용자별 일일 캡 |
| SEC-5 | 대본 큐 스푸핑(desync) | Med | Confirmed | `useLiveKitRoom.ts:156-160`(sender 미검증)·`RoomPage.tsx:209-211`(주석 인지) | 참가자가 `script-cue` 직접 publish → 전원 텔레프롬프터 desync(초대방 griefing) | send-reaction과 동일 서버 릴레이 |
| SEC-6 | 일일한도 TOCTOU | Low | Confirmed | `trigger-vgen:103→133`(count→insert 비원자) | 동시 요청 소프트쿼터 우회(크레딧은 자기돈 원자차감) | count+insert RPC 원자화 |
| SEC-7 | 크레딧 RPC 클라 노출 | **High** | **Confirmed(실측)** | `20260704120002_create_credit_rpcs.sql:86-87`(`revoke from public`만) | anon 키로 `rpc/refund_credit(본인잡)`→무한 무료 생성 / `rpc/deduct_credit(피해자)`→크레딧 고갈 | `revoke from public, anon, authenticated`(재감사 2026-07-07) |
| SEC-8 | 레이트리밋 RPC 노출 | **Med** | **Confirmed(실측)** | `20260706130000_rate_limit.sql`(revoke 전무) | `rpc/check_rate_limit('vgen:<피해자>')` 반복→피해자 429 DoS·SEC-1/4/6 무력화 | 동상 완전 revoke(재감사 2026-07-07) |
| SEC-RES-1 | 예약 무제한 생성 + 무권한 초대(알림 스팸) | Med | Confirmed | `create-reservation:32-47`(rate-limit 0·invitee 관계검증 0) | 임의 유저 8명 `reservation_invite` 알림 주입·반복 스팸/harassment | `check_rate_limit` 일일캡 + invitee⊆최근동료 (델타 2026-07-08) |
| SEC-RES-2 | recent-people excludeRoomId 추론 | Low | Confirmed | `list-recent-people:19,40-46`(is_room_member 미게이트) | 내 동료 X의 방 R 재실 여부 diff 추론(범위 좁음) | excludeRoomId `is_room_member` 게이트 (델타 2026-07-08) |
| SEC-RA-1 | room-authority broadcast 스푸핑 | Med | Confirmed(직접대조) | `useLiveKitRoom.ts:203-210`(script-cue/role/reaction 은 `if(participant)return` 방어·room-authority만 누락) | 악성 클라가 `mode_change`/`bg_change`/`vod_sync` 위조 publish→전원 UI desync·영상점프 (promoted→canPublish·vgen→getVgenUrl·kicked→PARTICIPANT_REMOVED 3중 서버재검증으로 **권한상승·유출은 차단**) | 서버전용 타입(promoted/mode_change/bg_change/script_mode/kicked/stage_invite/raise_hand)은 participant=undefined만 수락 + 클라 호스트타입(vod_sync/vgen_*)은 호스트 identity 대조 or 토픽분리 — SEC-5 잔여 (델타 2026-07-10) |
| SEC-AVJ-1 | create-avatar-job 레이트리밋·크레딧 부재 | Med(Modal 라이브 시 High) | Confirmed | `create-avatar-job:39-45`(rate-limit 0·credit 0) | 봇 무한 잡→Modal GPU spawn 무한→무과금 비용(IDOR는 isSafeObjectKey authId 고정으로 차단) | `check_rate_limit('avatar-job:<user>',N,3600)`=SEC-4 패턴. 표면 라이브(`/atelier-forge`·CommissionCorner) (델타 2026-07-10) |
| SEC-RXN-1 | send-reaction 서버 레이트리밋 부재 | Med | Confirmed(ponytail 주석) | `send-reaction:7`(클라 5/s만·서버 0) | 봇 방참가 후 초당 수백 reaction→전원 broadcast/렌더 폭탄(멤버십 게이트는 있음) | `check_rate_limit` room+user 캡 (델타 2026-07-10) |
| SEC-CR-1 | create-room 방생성 레이트리밋 부재 | Low(griefing) | Confirmed | `create-room:37`(방생성 rate-limit 0·notify만 캡) | 방 무한생성→로비 오염·rooms 비대(유령방·유출 없음) | `check_rate_limit('room-create:<user>',5,3600)`·API-SURFACE account_restrictions 미구현 설계갭 (델타 2026-07-10) |
| SEC-CR-2 | create-room language 미검증 | Low | Confirmed | `create-room:31`(길이·화이트리스트 0·genre는 검증) | 임의/장문 문자열 저장(React escape로 현 XSS 아님) | genre 동형 화이트리스트 `["ko","en","ja"]` (델타 2026-07-10) |

## §2. 보안 — 반증 (안전 확인·거짓양성 억제)

- **vgen-webhook**: ED25519 서명 + ±5분 타임스탬프 → 위조·재생 불가 (정찰 1순위 의심이 실제론 견고)
- **deduct/refund**: `FOR UPDATE` 원자 + `credit_deducted_at`∧`credit_refunded_at` 멱등 → 이중지불/환불 불가
- **호스트 인가**(kick/mute/set-password: server-derived userId) · **멤버십 게이트**(reaction/list-members/consent) · **`.single()`**(!appUser로 null 캐치)
- **RLS**(dub_*/credits/room_secrets deny-all + `is_room_member` + `current_user_id`) · **presign IDOR**(멤버 확인 후 SigV4) · **room_secrets 비번해시 미노출**
- **재감사 2026-07-07 반증**: `list-room-members`의 `muted_by_host` 전멤버 노출은 **의도됨**(A-FUNC-3: 음소거된 본인이 상태 인지·배지 렌더에 필요, 민감도 낮음) · `seed_user_credits`는 **트리거 함수**(PostgREST RPC 미노출·`on conflict do nothing` 멱등)라 무해 · `reconcile_stuck_vgen_jobs`는 노출되나 내부 가드(`refund_credit` 멱등·`skip locked`·차감 후에만 환불)로 실익 0 · `vgen-webhook` 차감선행이라 webhook-race 무료생성 불가 · `join_room_as_participant`는 `public,anon,authenticated` **완전 revoke**(SEC-7/8 정수정의 기준 패턴)

## §3. UX 마찰 (상세: design/UX-GAPS-AND-PATTERNS.md)

- **Blocker**: 뷰어/모바일 경로 구현 0(계약서만) · 리액션 우클릭 전용→터치 불가 · 룸 조인 타임아웃/취소 없음 · 비번찾기/인증재전송 없음
- **High**: 무대 비반응형(고정120px·`sm:`0회) · DUB 수동 새로고침 · VGEN 진행도/ETA 없음  *(※ text-muted 대비는 WCAG 실측 7.2:1로 AA 통과 — 정찰 오탐 정정)*
- **Med**: 강퇴 2단토글(모달아님·사유없음) · 연결품질 이모지만 · mute desync · 호스트 이양 미배선 · 다국어 15%

## §4. 있으면 좋을 기능 · 잘되는 점

- **기능**: 게스트/미리보기 모드 · 비번리셋·인증재전송 · 세션 녹화 · 밴(≠강퇴) · 대기실 · 룸설정 편집 · 키보드 리액션 핫키 · 색맹안전 상태 라벨
- **잘됨(회귀금지)**: a11y 시맨틱 · auth 세션 복원+원목적지 복귀 · VGEN 코스트 프리뷰 · 2계층 mute · 비번락 정합 · DubCompositor 진행도 · 서버 릴레이 리액션(스푸핑 차단)

---

## 진행 로그

- **2026-07-06** dogfood-audit 최초 실행(6 페르소나). 발견 12 actionable + 반증 7. 백로그 §0 등록.
- **2026-07-06** SEC-1 구현: rate-limit 프리미티브(마이그+RPC) + 잠금방 적용. 격리 postgres 17.6 실측 PASS(5허용→6차단→400s후 리셋→키격리→RLS). 배포 미실행(승인 대기).
- **2026-07-06** SEC-2 구현: 공용 `isSafeObjectKey` + 3함수 적용. deno check clean + 헬퍼 로직 11/11 PASS(정상 통과·traversal 전부 차단). 배포 대기.
- **2026-07-06** UX-P6 **거짓양성 정정**: WCAG 실측으로 muted 대비 7.21:1(AA 통과) — 정찰의 3.2:1 오산. 토큰 무변경, 그림자 폴리시는 P2로.
- **2026-07-06** **트랙분리**: §0을 트랙 A(로직·보안·seam=지금·`/goal` 대상)/트랙 B(UIUX 표현=나중 1회 패스)로 재편. UX 항목은 seam(A)/표현(B)으로 분해. `/goal` 명령어가 기본 트랙 A만 밀도록 갱신.
- **2026-07-06** `/goal A` 실행: **SEC-3**(SSRF, `isOwnR2RefUrl` R2 오리진 allowlist, 11/11) + **SEC-4**(비용 API 캡, check_rate_limit 4함수 재사용, deno clean ×4) 구현·검증. 배포 대기.
- **2026-07-06** **자기리뷰**: SEC-2 업로드 키 프리픽스 3종(sources/recordings/outputs) 실코드 대조 — 정상 흐름 무손상 확인. SEC-1 오탐(성공시도 계수) 발견 → **정답 시 카운터 리셋** 추가·DB 실측. join-room deno check는 기존 password.ts 타입에러로 막힘(내 코드 무관, DB로 대체 검증).
- **2026-07-06** **SEC-5**(대본 큐 서버 릴레이): 새 Edge `advance-script-cue` + 클라 4배선(rooms 래퍼·receiver 스푸핑방어·sendCue 릴레이·handleCue self-echo 게이트). type-check·lint·test 50/50·deno clean. 배포 대기. **보안 SEC-1~5 전부 구현·검증 완료**(SEC-6 TOCTOU만 A-P2 잔여). 다음=A-SEAM-1(UX 로직 seam).
- **2026-07-06** **A-SEAM-1**(피드백 채널 seam): `stores/toastStore.ts`(큐+4s 자동소멸 store 소유) + `hooks/useToast.ts`(`toast`/`useToast()` emit API) + `tests/unit/toastStore.test.ts`(3) 신설, cue relay 침묵 swallow → `toast.error(i18n.t('room.cueSyncFailed'))` 배선(ko 키 1). type-check·lint clean·**test 53/53**·build PASS. 초안의 no-op/console 대신 실 큐 채널로 격상 → 트랙 B는 `<ToastHost/>` 렌더만(타이밍 로직 불필요). 배포 불필요(프론트, CF Pages 재배포로 라이브). 다음=A-SEAM-2(진행/ETA 데이터 노출).
- **2026-07-06** **CLAUDE.md 교정**: "Phase 0 코드 없음"(stale) → "구현 진행 중·Phase 0~3 대부분 충족·보안+seam 단계"로, 먼저읽을것에 DOGFOOD-AUDIT §0 최우선 백로그 추가, features 목록·MILESTONES 포인터 갱신.
- **2026-07-06** `/goal A` 연속 실행(트랙 A P1): **A-SEAM-2**(VGEN `estimateVgenSeconds`+`currentJobEtaSec` 노출·DUB는 기존 로컬상태, test 56/56) → **A-SEAM-3**(`useRealtimeRow` 훅·DUB 2구독·수동버튼 제거·컬럼 실대조, test 56/56) → **A-FUNC-1**(callFn 3중복→공유 edgeFn+15s 타임아웃/취소, test 58/58) → **A-FUNC-2**(인증 복구 3액션·LoginPage/RegisterPage 배선·/reset 페이지·비번규칙 공유추출, test 62/62). 각 type-check·lint·build PASS. **트랙 A P1 완료.** 프론트라 배포 불필요(CF Pages 재배포로 라이브·A-SEAM-3 realtime/A-FUNC-2 이메일은 배포후 실측). 다음=A-P2(SEC-6·A-SEAM-4·5·A-FUNC-3).
- **2026-07-07** **`/goal SEC-7 배포까지 프로드노출확인` — SEC-7·SEC-8 수정+프로드 배포+실측 완료.** 새 마이그 `20260707120000_lock_rpc_execute.sql`(check_rate_limit·deduct_credit·refund_credit·reconcile_stuck_vgen_jobs 를 `public,anon,authenticated` 완전 revoke; RLS 헬퍼 current_user_id/is_room_member/is_dub_member 는 의도적 유지). **격리 pg17.6.1.127 실측**(BEFORE 4함수 auth=t → AFTER anon/auth=f·**svc=t** = Edge 무손상). **프로드 실측**(psql pooler): 배포 **전** `deduct_credit`·`refund_credit` anon=t·auth=t = **라이브 노출 확정**(크레딧 마이그는 이미 배포돼 있었음)·`check_rate_limit` 프로드 미존재 → `supabase db push`(마이그 2개: rate_limit + lock) → 배포 **후** 5함수 전부 anon=f·auth=f·svc=t. **SEC-7 라이브 구멍 차단 완료.** 마이그 파일 미커밋(git 커밋 승인 대기 — 배포본-소스 드리프트 방지 위해 커밋 권장).
- **2026-07-07** **재감사(`/dogfood-audit`)**: 정찰 3종(하이쿠 인벤토리+소넷 보안표면+소넷 UX여정) → 메인 직접 검증(성역). **신규 확정 2건**(격리 pg17.6.1.127 `has_function_privilege` 실측): **SEC-7**(High, `deduct_credit`·`refund_credit` `revoke from public`만 → Supabase default-priv가 anon/authenticated에 EXECUTE 명시부여라 revoke 무효, `auth ... =t` 실측 → 무한 무료생성/크레딧고갈) · **SEC-8**(Med, `check_rate_limit` revoke 전무 → anon/auth EXECUTE=t → 피해자 429 DoS·SEC-1/4/6 무력화). 정수정=`join_room_as_participant` 패턴(`revoke from public, anon, authenticated`) 신규 마이그 1개. **반증**: muted_by_host 노출(의도)·seed_user_credits(트리거·멱등)·reconcile(가드로 실익0)·webhook-race(차감선행)·advance-script-cue 퇴장후 브로드캐스트(Low, 호스트 자기방). UX 발견은 전부 트랙 B 기존 항목(스피너·ToastHost 미마운트·에러 auto-dismiss·진행바·i18n·모바일리액션)에 흡수. **⚠️ 크레딧 마이그는 이미 프로덕션 배포 가능성 → SEC-7 라이브 노출 여부 prod 확인 필요.**
- **2026-07-07** `/goal A` 트랙 A **P2 완주**: **SEC-6**(trigger-vgen 일일한도 count→insert 비원자를 check_rate_limit 원자화, deno clean) → **A-SEAM-4**(뷰어 권한 seam=roomStore `myRole`+순수 `roomPermissions`; 서버는 이미 canPublish 강제·전체 ViewerGate/MobileViewer/invite Edge/anon 은 P2 기능 defer, test 64/64) → **A-SEAM-5**(i18n `coverage.ts` missing/orphan + 가드 테스트=en/ja⊆ko 확인·B worklist, test 67/67) → **A-FUNC-3**(호스트 이양 host_id 기반+폴백[무배포]·mute 마운트 로드[list-room-members muted_by_host, 배포 게이트], test 67/67·deno clean). **트랙 A 전 항목(P0~P2) 완료.** 배포 대기 추가분: trigger-vgen(SEC-6)·list-room-members(mute). 남은 것 = 트랙 B(UIUX 프레젠테이션 1회 패스)뿐.
- **2026-07-08** **트랙 B P0 3종 완료(주인님 목업 검수 승인 후 구현)**: 실토큰 인터랙티브 목업(Artifact)으로 사전 검수 → 채택안 = 0-의존성 CSS/SVG 마이크로 모션(Lottie 플레이어·스프라이트 도입 없음, 글리프는 컴포넌트 경계로 격리해 spritegen 교체 대비). **①ToastHost**(전역 마운트·의미색 고정·체크 드로우·에러 쉐이크) **②ProgressBar+VGEN ETA+DUB 단계 바**(`--scene-accent` 연동·95% 캡·불씨 선단) **③조인 취소**(joinRoom signal 관통·모닥불 글리프·"모닥불에 다가가는 중…" 카피). **실측:** tsc0·lint clean·test 70/70(+3 etaProgress)·build + **헤드리스 실렌더 13/13**(토스트 3종/roles/4.2s 소멸/360px · route 홀드→취소→로비 · VGEN 바/aria/ETA 문구) + 스크린샷 육안. 함정 기록: dev HMR 후 evaluate 모듈 주입은 `?t=` 이중 인스턴스 → **서버 재시작 후 검증**. 배포 불필요(프론트 — CF Pages 재배포로 라이브). 다음 = 트랙 B 잔여(반응형·모바일 뷰어·다국어·모달/그림자).
- **2026-07-08** **트랙 B 연속 2건 — 다국어 완역 + 반응형 P-5.** ① en/ja 232키 전량 번역(coverage 0/0·test 70/70·실렌더 EN/JA 확인; i18n 폴백 테스트는 합성 프로브 키로 정수정). ② P-5: 무대 동적 슬롯(88/120, `useSlotPx` — 아바타 캔버스는 size 가 이펙트 deps 라 재생성 안전) + 터치 롱프레스→휠 sticky 개화(합성 mousedown 억제 함정 처리) + 숫자키 핫키 + 검색폭/패딩 반응형. 헤드리스 실측 9/9 + 360px 스크린샷. **모바일 뷰어는 표현이 아니라 기능 게이트**(뷰어 입장 경로 미구현)로 판정 — 뷰어 조인 슬라이스와 함께. 커밋: 트랙 A seam(1e9bdc7)·아트 피벗(2bd2a3f)·트랙 B P0(054d44b) + 이번 2건 별도 커밋. 참고: 토스트 개통으로 **advance-script-cue 미배포가 가시화**(호스트 입장 시 cue 전송 실패 토스트) — Edge 배포 대기 목록의 실사용 동기.
- **2026-07-08** **델타 감사(`/dogfood-audit`, 신규 표면 한정)**: 지난 감사 이후 신규 Edge 7(초대·뷰어·예약·최근사람)+마이그 6 대상. 페르소나 워커 3 병렬(초대·뷰어 보안 / 예약·알림·최근사람 보안 / 로비 v3 UX) → 메인 직접 원본 대조(성역). **초대·뷰어 = 반증(견고)**: 128-bit 코드·SHA-256 해시·deny-all RLS·verify 5회/5분·consume `FOR UPDATE` 원자·뷰어 정원제외(`role<>'viewer'`)·escalation unique index. 신규 RPC 3(consume_room_invite·join_room_as_viewer·send_reservation_reminders) 전부 3중 revoke([[supabase-definer-rpc-revoke]] 반영). **신규 확정 2건(예약)**: SEC-RES-1(Med, `create-reservation` rate-limit 0 + 무권한 초대→임의유저 알림 스팸)·SEC-RES-2(Low, `list-recent-people` excludeRoomId 추론). 워커 High→실임팩트 Med 강등(외부비용 없음). **UX**: 대부분 트랙 B 기존 잔여(반응형·이미지 로딩 스켈레톤) 흡수, 신규 실질=초대 에러 세분 배선(verify-invite-code 가 410 revoked/expired/used_up 세분 응답 주는데 LobbyPage catch 가 단일 `inviteInvalid` 로 뭉갬 — 백엔드 seam 존재)·모바일 하단네비 active state 부재(LobbyPage:195-206). **오진 기각**: TeahousePage `☕` 이모지가 i18n 게이트 깬다(이모지≠한글·lint 미매칭·이미 `title={t('teahouse.chipHint')}`)·예약 성공 피드백 없음(`reserveDone` 토스트 존재 TheaterPage:245)·초대 20명(MAX_INVITEES=8). §0 트랙 A-P1b + §1 표 등록. **미구현·커밋 승인 대기**(문서만 갱신).
- **2026-07-08** **델타 감사 발견 개선 구현(주인님 "발견한것들 개선해줘")**: **SEC-RES-1**(create-reservation 에 `check_rate_limit` 일일 캡 20 + invitee 를 공유 room_participants 이력 ∩ 미삭제 유저로 게이트·notified 실발송수 정정) → **SEC-RES-2**(list-recent-people excludeRoomId 를 내 참가 이력 `roomIds.includes` 게이트, 추가쿼리 0) → **UX 초대 에러 세분**(LobbyPage catch 가 edgeFn 보존 error.message 로 revoked/expired/used_up/room_ended 원인별 안내, i18n 4키 ko/en/ja 완역). **실측:** deno check clean(2함수)·type-check 0·lint clean·test 75/75(i18nCoverage 포함)·build PASS. **모바일 네비 active state 는 기각**(네비 non-persistent). **배포 완료(2026-07-08):** `create-reservation`·`list-recent-people` functions deploy + 프론트 CF Pages 재배포(`8896a4ab`). **실측:** Edge auth 게이트 401 · 번들 별칭 `index-n4BKua7C.js` dist=alias MATCH. 커밋 0aad8d8(수정)·<docs>(배포반영). 라이브 예약 흐름 E2E(rate-limit·관계 게이트 실동작)는 실계정 필요 — RPC 프리미티브는 SEC-1 격리 실측으로 대체.
- **2026-07-10** **룸 페이지 페이즈루프 P1~P8(Fable, 8커밋)**: Phase 3 AC 소품(①ROOM-06 글자크기 `023e624` ②배경 6종 확충) + 계약 갭(④G-261 모드 broadcast `d049791` ⑤G-64 Self-PiP `17ac919` — G-63 즉흥은 **기능 게이트 defer 판정**: `rooms.script_id` 부재로 분기 도달 불가) + UX·P1(③채팅 타임스탬프·전송확인 `38280ef` ③b강퇴 사유 Edge 슬라이스 `ae32317` ⑦ROOM-08 믹서 `bd04550` ⑧ROOM-01 타임라인 동기 ±200ms `b112bed`). **병행(의상실) 세션과 같은 워크트리** — 공유 파일(RoomPage·useLiveKitRoom·MainView·index.css·locale 3)은 HEAD 재구성 선별 스테이징으로 무오염 커밋(매회 staged/unstaged numstat 실측). 게이트 tsc0·lint·test 112/112(+25)·docs:check 그린, build 는 외부 dev 서버 가동으로 보류. **배포 대기(승인 게이트)**: db push(`current_mode`)·Edge 2종(kick-participant·set-room-mode)·CF Pages + 라이브 E2E 번들(강퇴사유 2탭·모드배너/자동탭·PiP 실렌더·믹서 무음·vod ±200ms·kick 통합테스트 실행).
- **2026-07-10** **배포+라이브 E2E 완료(주인님 "배포 커밋푸시 e2e")**: push(9커밋) → db push(`20260710093000_add_room_current_mode`) → Edge deploy(kick-participant v4·set-room-mode v1 ACTIVE 실측) → CF Pages `b69b3be7`(번들 비밀키 감사 6키+service_role 클린·dist=서빙 별칭 MATCH·root/asset/deep 200·헤드리스 React 마운트/콘솔 0). **kick 통합테스트(프로드) 6/6**(403/400/200+긴 reason 클램프·is_disabled·token_version+1·재입장 403). **배포판 2탭 E2E 12/12**(e2e-aria 계정 비번 admin 리셋·세션 주입·fake media): 모드 broadcast(배너 "🤖 VGEN 모드 진행 중"·VGen 탭 aria-selected·DB current_mode=vgen)·Self-PiP 개화·믹서 원격 볼륨 1→0 실측(`[data-lk-audio].volume`)·강퇴 사유 피강퇴자 화면 표기(스크린샷 육안) — 양탭 콘솔 에러 0. **잔여**: vod ±200ms 라이브 실측(영상 콘텐츠 필요 — 다음 실제 VGEN 세션에서, MILESTONES:86 체크 보류)·배포판에 병행 세션 미커밋 리디자인 포함(워크트리 빌드 — 그쪽 커밋 필요). 함정 재확인: 셸 stale SUPABASE_ACCESS_TOKEN → .env 토큰 인라인 / rtk npx 재작성 → `/opt/homebrew/bin/npx` 절대경로.
- **2026-07-10** **수직 3종 배포+라이브 E2E(주인님 "배포 ㄱ")**: db push(`20260710150000_create_friendships`) → Edge 6종 deploy(친구 5 v1 + create-room v8 ACTIVE 실측) → CF Pages `caa74fa9`(비밀키 감사 클린·dist=별칭 MATCH·200×3). **라이브 E2E 17/17**(프로드): 친구 매트릭스(자기 400·요청 201·멱등 200·역방향 409 incoming_exists·수신함 노출·발신자 수락 403·수락 200·**미러 행 DB 실측**·이름 해석)+팔로우(200·**공연시작 알림 payload.room_id 실측**)+배포판 2탭(광장 칩 클러스터 [친구·언어·로그아웃] 렌더·**B 패널에 A 온라인 초록점→A 방 입장 시 '공연 중' 전환** — presence 재트랙 실증, 스크린샷 육안). 정리: e2e 방·관계·알림 삭제, 키 파일 삭제, playwright 제거.
- **2026-07-10** **델타 도그푸딩 감사(`/dogfood-audit`, 신규 표면 한정)**: 정찰 3(하이쿠 인벤토리+소넷 보안표면+소넷 UX여정) → 메인 직접 검증(성역). **확정 취약점 0.** 발견·판정: **DP-1**(Med, `friends_presence` 전역 채널 노출 → 근본 재설계 착수) · **OPS-1**(운영 Med, untracked 20파일 프로덕션 라이브 → 커밋) · UX 4(알림 데드엔드·신규유저 친구 고립·VGEN 피드백·수락 로딩). **반증(거짓양성 억제)**: `check_rate_limit` revoke 누락(정찰 ⚠️)=**Refuted**(SEC-8 `20260707120000_lock_rpc_execute.sql:17`로 이미 3중 revoke, 프로드 실측 anon=f·auth=f·svc=t — 정찰이 lock 마이그 미확인) · RoomShell "미구현 Blocker"(정찰 2건)=**오판**(정찰이 orca 뒤처진 워크트리를 봄, jason 엔 존재·빌드·배포됨). **잘됨(회귀금지)**: 친구 미러행·rate-limit·멱등 서버 강제·friendships 쓰기정책 부재·avatar 버킷 격리·dev 훅 전부 가드·i18n 완전동기.
- **2026-07-10** **감사 개선 착수(주인님 "착수 플랜"→승인)**: **OPS-1**(`6ef2061`): untracked 8파일(RoomShell·TopBar·BottomBar·DirectorNotesTab·HostCrown·StreamPage·notesStore+테스트) git 추적 — RoomPage/App 필수 의존, 게이트 그린 후 명시 커밋. **DP-1 근본 재설계**(`3c34e6d`): 전역 채널 폐기 → `users.last_active_at`(마이그 `20260710160000`) heartbeat 30s + `list-friends` 친구관계 검증 후 online/activity 서버 반환(전역 노출 0) + FriendsButton 15s 폴링. **UX 4건**(`ebd9bd5`): 알림 방상태 재검증·신규유저 대극장 CTA·vgenErrorKey 원인 매핑·수락 로딩. 게이트 tsc0·lint·test 120/120·docs:check.
- **2026-07-10** **감사 개선 배포+라이브 실측(주인님 "문서업데이트하고 배포")**: db push(`20260710160000_add_user_last_active`) → Edge `list-friends` v2 재배포 → CF Pages `cdd64154`(비밀키 감사 클린·별칭 전파 후 dist=서빙 MATCH·200×3). **presence 재설계 서버 경로 probe 7/7**(프로드): heartbeat UPDATE(RLS 본인 허용·컬럼 실재)·online 60s 윈도·activity room/lobby·**list-friends 는 친구만 반환=전역 노출 0 구조 확인**·stale(2분전)→offline. 친구 흐름 배포판 E2E 15/17(매트릭스·칩 클러스터 전부 통과). **presence 브라우저 2건은 옛 전역-채널 즉시-sync 가정이라 실패 — 기능은 probe 로 확정, E2E 는 heartbeat+15s 폴링 대기로 갱신 대상(테스트 낡음, 기능 정상).** 정리: 키 파일·playwright 제거.
- **2026-07-10** **델타 도그푸딩 감사(`/dogfood-audit`, 룸 리디자인+이모트+승격경로+아바타포지 표면)**: 정찰 3(하이쿠 인벤토리+소넷 보안표면+소넷 UX여정) → Opus 페르소나 워커 3(악성참가자 승격·경제자원·UX복합) 병렬 → 메인 직접 원본 대조(성역·심각도 실임팩트 재조정). **신규 확정 5건**(§1 표 등록): **SEC-RA-1**(Med, 수신핸들러가 `if(participant)return` 스푸핑방어를 script-cue/role/reaction 3토픽에 넣고 **room-authority만 누락** → mode_change/bg_change/vod_sync 위조 desync·griefing; **권한상승·유출은 canPublish/getVgenUrl/PARTICIPANT_REMOVED 3중 서버재검증으로 차단** → 워커 High를 Med 강등, SEC-5 잔여)·**SEC-AVJ-1**(Med, create-avatar-job rate-limit·credit 0 → Modal GPU 무과금 비용, 표면 라이브 `/atelier-forge`)·**SEC-RXN-1**(Med, send-reaction 서버 rate-limit 0·ponytail)·**SEC-CR-1**(Low, 방생성 rate-limit 0 griefing)·**SEC-CR-2**(Low, language 미검증). **반증(거짓양성 억제)**: 승격 3단사슬 자기승격(raise-hand 본인토글·invite 호스트+손든viewer·accept promote_viewer RPC 3중revoke)·호스트권한 클라신뢰(getAppUser server-derived+host_id 대조)·cross-room(멤버십 이중조건)·sync-script-role 배역탈취(claim authId 서버확정+수신 participant 가드)·ensure-studio-room(uniq_studio_per_host)·avatar_jobs RLS/IDOR(current_user_id+isSafeObjectKey authId 고정)·kicked/promoted/vgen 위조(전부 서버 재검증). **UX**: toast 미배선 3(링크복사 `RoomPage:711`·로드아웃·리액션실패)·ReactionOverlay reduced-motion·ReactionWheel 키보드탐색·온보딩 데드엔드 — 대부분 트랙 B. **잘됨(회귀금지)**: 스푸핑 방어 4토픽 일관(room-authority 예외로 발견)·승격 다층 재검증·EmoteConsole `disabled={!connected}`·휠 뷰포트 클램프. §0 A-P1c + §1 표 등록. **미구현·커밋 승인 대기(문서만).** GAP-MATRIX 진행로그는 병행세션 dirty로 유보.
- **2026-07-15** **의상실 3연속 배포 + 배포대기 flush 실측(주인님 세션)**: ① **다포맷 업로드**(`8ed344c`): 버킷 `allowed_mime_types` png→png/webp/jpeg(마이그 `20260715120000`, db push+psql 실측)·avatarJobs 동적 확장자·CommissionCorner `validateImage` — Modal 이 PIL 매직바이트로 포맷무관 디코드 확인해 **백엔드 0변경**(라이브 WebP/JPEG 200·GIF 400·번들 시크릿 감사 클린·CF Pages served=built). ② **인플라이트 디덥**(`58c4ecc`): 부분 유니크 인덱스 `(user_id,input_hash) WHERE status in(queued,running)`(마이그 `20260715130000`) + `create-avatar-job`(v7) 조기반환/23505 백스톱 — 같은 그림 동시제출 GPU 이중과금 차단(프로드 라이브 23505 롤백txn). ③ **입 과대개구 3층수정**(`2db4931` QA·Vtube `3a85af2` 클램프): `qa-mouth-lips` 진폭 축(유효개구/얼굴>0.13, wide_grow 도 판정)·`rig_keyforms` 얼굴비례 H-클램프(0.12, wide_grow 전용)·modal deploy(chatterbox-rig, `modal_rig_app.py:88`이 qa-mouth-lips 컨테이너 복사) + 99c74a83 응급 재업로드(유효개구 17.7%→12.2%·QA PASS·라이브 실측). ④ **배포대기 flush = 중복 판정(정직 정정)**: SEC-KICK-1/2(데이터접근5)·SEC-DUB-1 은 **이미 2026-07-14 배포+실증**(flush 직전 버전 send-chat v4·getter v10/10/9/3/6 = 백로그 line 52/54 기록과 정확히 일치) — 07-14 12:30 워크트리배포 vs 21:38 `94bf5d4` 커밋 시차를 드리프트로 **오판**, 7함수 동일코드 재배포(v bump만·기능변화 0·무해). **실 미배포 델타 = 0.** **잔여(미착수)**: SEC-KICK-2 형제 ~14함수(advance-script-cue·sync-script-role·invite/accept-stage·submit-viewer-poll·record-consent·record-recording-consent·leave-room 등 저심각도) · Vtube 레버(Modal-kill↔DB 유령잡 pg_cron 워치독[병행세션 착수, 마이그 `20260715190000`]·잡중 ~300MB 이미지리빌드·VLM 스펙 비결정성).
- **2026-07-17** **룸 페이지 누락기능 감사(`/감사`)**: 정찰 3(하이쿠 인벤토리+소넷 스펙지도+소넷 UX여정) → Opus 페르소나 3(호스트·배우·뷰어/모바일/a11y) → 메인 원본 대조(성역). **확정 갭 6 + 대형 1**(§0 A-P1e 등재: RM-HOSTXFER·RM-EDIT·RM-GUEST-CTA·RM-MUTE-DUR·RM-SOFTLEAVE·RM-SCRIPT) · UX 마찰 6(트랙 B 델타) · **손들기 뷰어 버튼 복원은 주인님 결정으로 폐기**. 오탐 기각 다수(ORPHAN 16 전부 배선·배속 칩 구현·모바일 하단바 고정·스펙지도 "미착수" 6건 stale — 문서 드리프트로 별도 기록). 신규 보안 발견 0(4차 감사까지의 하드닝 유지 확인).
- **2026-07-17** **A-P1e 구현 완주(`/루프` 첫 실주행 — GOAL-room-gaps R1~R5·R7, 6/7)**: 발견→§0 등재→골 사다리 조립→phase-loop 자율 실행 파이프라인 실증. RM-HOSTXFER·RM-EDIT·RM-GUEST-CTA·RM-MUTE-DUR·RM-SOFTLEAVE + UX 델타 6종 전부 [x](각 행 참조). 매 phase check:all 그린(159/159)·자기리뷰 PASS·FAIL 3회 정수정 사이클 실작동. **RM-SCRIPT 는 승인 게이트서 HOLD**(주인님 결정 — 스펙 정본(G-286·CNT-02·09) 재설계). ⏳ 배포 대기 일괄: Edge 신규3+수정4·config·CF Pages·LiveKit webhook 등록·라이브 2탭 실증(상세 GAP 진행로그 2026-07-17).
- **2026-07-17** **좀비 방 정리 + RM-REAP reaper 배포(주인님 "테스트 방 다 제거 + 룸 감사")**: 프로드 실측 비-ended 21방 중 15 stale 테스트방(4.7~14일·전부 test/e2e/dasf) `status='ended'` 수동 정리(로비 17→2, 라이브 거마비·연습방만 유지) — LiveKit 실접속 대조로 실사용 1방 보호. **근본 = 종료가 softLeaveRoom(마지막 배우 나감)에만 의존·미접속/미회수 방 회수 크론 0.** 정수정 배포: Edge `reap-stale-rooms`(v3, REAPER_SECRET 가드·LiveKit presence 0 방만 종료) + 마이그 `20260717120000`(pg_net 0.20.3 설치·cron `reap-stale-rooms` */5·이 레포 첫 크론→Edge, vault `reaper_secret` bearer). **라이브 실증**: `lk room join` positive-control(실접속=reaped:0 보호 / SIGKILL~20s=reaped:1 회수)·bearer 401·크론 net._http_response 200. (§0 A-P1e RM-REAP 행.)
